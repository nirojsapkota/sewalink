---
phase: 01-foundation-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Gemfile, config/application.rb, config/locales/en.yml, config/locales/ne.yml, app/controllers/application_controller.rb, app/views/layouts/application.html.erb]
autonomous: true
requirements: [AUTH-04]
user_setup: []

must_haves:
  truths:
    - "Application is initialized with PostgreSQL and Tailwind CSS"
    - "UI text is translated to Nepali when ?locale=ne is provided"
    - "User language preference persists in the session/cookie"
  artifacts:
    - path: "config/locales/ne.yml"
      provides: "Nepali translations"
    - path: "app/controllers/concerns/localizable.rb"
      provides: "Locale setting logic"
    - path: "Gemfile"
      contains: "rails-i18n"
  key_links:
    - from: "app/controllers/application_controller.rb"
      to: "app/controllers/concerns/localizable.rb"
      via: "include"
---

<objective>
Initialize the sewaLink Rails application and establish the bilingual (English/Nepali) foundation.

Purpose: Set the technical stage for a localized marketplace.
Output: Working Rails app with i18n support and language toggle capability.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-identity/01-CONTEXT.md
@.planning/phases/01-foundation-identity/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Rails application with PostgreSQL and Tailwind CSS</name>
  <files>Gemfile, config/database.yml, config/application.rb, app/views/layouts/application.html.erb</files>
  <action>
    - Run `rails new . --database=postgresql --css=tailwind --force` to initialize the project in the current directory.
    - Configure PostgreSQL connection details for development.
    - Add `rails-i18n` to Gemfile.
    - Add `Mukta` font to Tailwind configuration and application layout per D-04 research.
  </action>
  <verify>
    <automated>bundle exec rails -v && grep "rails-i18n" Gemfile</automated>
  </verify>
  <done>Rails app initialized and bundle installed</done>
</task>

<task type="auto">
  <name>Task 2: Implement Bilingual (EN/NE) core and toggle logic</name>
  <files>config/locales/en.yml, config/locales/ne.yml, app/controllers/concerns/localizable.rb, app/controllers/application_controller.rb</files>
  <action>
    - Create `config/locales/ne.yml` with basic Nepali translations (e.g., hello, login, register).
    - Implement `Localizable` concern to handle `I18n.locale` based on params, session, or user preference (D-04, D-06).
    - Include `Localizable` in `ApplicationController`.
    - Add a simple language toggle link to the main layout for verification (D-05).
  </action>
  <verify>
    <automated>
      # Verify that ne.yml exists and contains 'ne:'
      grep "^ne:" config/locales/ne.yml && 
      # Verify locale setting logic via a simple controller test if possible
      echo "Locale logic implemented"
    </automated>
  </verify>
  <done>Language toggle works and persists across requests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → application | User-provided locale parameters cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | Locale Param | mitigate | Validate that provided locale is within supported set ([:en, :ne]) |
</threat_model>

<verification>
Run `bin/rails server` and visit `/?locale=ne`. Verify that the UI text changes to Nepali.
</verification>

<success_criteria>
- Rails app is running on PostgreSQL.
- English and Nepali locales are supported.
- Language preference is stored and retrieved correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-01-SUMMARY.md`
</output>
