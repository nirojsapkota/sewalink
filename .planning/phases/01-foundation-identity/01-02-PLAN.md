---
phase: 01-foundation-identity
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [Gemfile, Gemfile.lock, app/models/user.rb, db/migrate/*.rb, app/services/sms_service.rb, config/routes.rb, config/initializers/rack_attack.rb, app/controllers/users/sessions_controller.rb]
autonomous: true
requirements: [AUTH-01, AUTH-02]
user_setup: []

must_haves:
  truths:
    - "User can initiate login with a 10-digit phone number"
    - "OTP is generated and intercepted by a mock SMS service in development"
    - "User can successfully authenticate by providing the correct 6-digit OTP"
    - "User can optionally log in with email/password if configured"
  artifacts:
    - path: "app/models/user.rb"
      contains: ["devise", "two_factor_authenticatable"]
    - path: "app/services/sms_service.rb"
      provides: "SMS delivery abstraction"
    - path: "config/initializers/rack_attack.rb"
      provides: "Rate limiting for OTP endpoints"
  key_links:
    - from: "app/models/user.rb"
      to: "app/services/sms_service.rb"
      via: "callback or service call"
---

<objective>
Implement the core identity and authentication layer using SMS OTP as the primary method and email/password as secondary.

Purpose: Secure and localized user access.
Output: Working authentication system with SMS OTP and rate-limiting.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-identity/01-CONTEXT.md
@.planning/phases/01-foundation-identity/01-RESEARCH.md
@.planning/phases/01-foundation-identity/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Devise and User model with phone and role support</name>
  <files>Gemfile, app/models/user.rb, db/migrate/*_devise_create_users.rb</files>
  <action>
    - Add `devise`, `devise-two-factor`, and `rack-attack` to Gemfile.
    - Generate Devise User model.
    - Modify the migration to include `phone` (string, unique, index), `email` (optional/nullable per D-02), and `active_role` (enum: poster, tasker, default: poster).
    - Run migrations.
  </action>
  <verify>
    <automated>bundle exec rails db:migrate && bundle exec rails runner "User.new(phone: '9841234567')"</automated>
  </verify>
  <done>User model with phone and active_role support created</done>
</task>

<task type="auto">
  <name>Task 2: Implement SMS OTP logic and SmsService</name>
  <files>app/services/sms_service.rb, app/models/user.rb, config/initializers/devise.rb</files>
  <action>
    - Configure `devise-two-factor` on the User model to use `phone` for OTP.
    - Create `app/services/sms_service.rb` with an adapter pattern (Sparrow/Mock) per research.
    - Implement logic to send OTP when a user attempts to log in with a phone number.
    - Configure `Rack::Attack` to rate-limit OTP requests per IP and phone number to prevent SMS pumping.
  </action>
  <verify>
    <automated># Run a unit test for SmsService or check for presence of service file
ls app/services/sms_service.rb && grep "OTP" app/models/user.rb</automated>
  </verify>
  <done>OTP generation and SMS delivery logic implemented</done>
</task>

<task type="auto">
  <name>Task 3: Create localized Auth views and flows</name>
  <files>app/views/devise/sessions/new.html.erb, app/controllers/users/sessions_controller.rb, config/routes.rb</files>
  <action>
    - Generate Devise views and customize the login form to accept either phone or email.
    - Implement a two-step login flow: Step 1 (Phone entry) -> Step 2 (OTP entry).
    - Ensure all strings in the auth views are localized using `t()` and present in `en.yml`/`ne.yml`.
    - Add a toggle in the UI to switch between Phone and Email login methods (AUTH-02).
  </action>
  <verify>
    <automated>grep -r "t(\".auth" app/views/devise/sessions/</automated>
  </verify>
  <done>Authentication UI with two-step OTP flow and localization is ready</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → application | User credentials and OTP codes |
| application → SMS Gateway | OTP codes for delivery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Denial of Service | OTP Endpoint | mitigate | Rack::Attack rate-limiting by IP and Phone Number |
| T-01-02-02 | Spoofing | Login | mitigate | Devise-two-factor lockout after N failed attempts |
| T-01-02-03 | Information Disclosure | Errors | mitigate | Use generic error messages (e.g., "Invalid credentials") |
</threat_model>

<verification>
Test the login flow manually: Enter a phone number, check logs for the mock OTP, enter OTP, and verify successful session creation.
</verification>

<success_criteria>
- Users can log in/sign up with phone number + OTP.
- Users can optionally use email/password.
- OTP requests are rate-limited.
- All auth UI is localized in English and Nepali.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-02-SUMMARY.md`
</output>
