---
phase: 01-foundation-identity
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [app/models/user.rb, config/routes.rb, app/controllers/profiles_controller.rb, app/controllers/onboarding_controller.rb, app/views/profiles/edit.html.erb, app/views/onboarding/show.html.erb, db/migrate/*.rb]
autonomous: true
requirements: [AUTH-05]
user_setup: []

must_haves:
  truths:
    - "New users are automatically redirected to the onboarding wizard"
    - "Users can update their name, bio, and profile picture"
    - "Users can toggle between 'Poster' and 'Tasker' roles"
    - "User language preference is saved to the database"
  artifacts:
    - path: "app/controllers/onboarding_controller.rb"
      provides: "Post-signup flow management"
    - path: "app/views/onboarding/show.html.erb"
      contains: "progress-bar"
  key_links:
    - from: "app/controllers/application_controller.rb"
      to: "app/controllers/onboarding_controller.rb"
      via: "redirect if !onboarded?"
---

<objective>
Complete the identity foundation by implementing user profiles and a lightweight onboarding wizard.

Purpose: Personalize user experience and capture essential preferences.
Output: Working profile management and multi-step onboarding flow.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-identity/01-CONTEXT.md
@.planning/phases/01-foundation-identity/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Onboarding Wizard</name>
  <files>app/controllers/onboarding_controller.rb, app/views/onboarding/show.html.erb, config/routes.rb, app/models/user.rb</files>
  <action>
    - Add `onboarded` (boolean, default: false) and `locale` (string) to the User model via migration.
    - Create `OnboardingController` to handle a multi-step form capturing: Name, Language preference, and Initial Role (D-09).
    - Add progress indicators to the onboarding views (D-10).
    - Implement a before_action in `ApplicationController` to redirect authenticated but non-onboarded users to the wizard.
  </action>
  <verify>
    <automated>bundle exec rails runner "User.new.respond_to?(:onboarded?)"</automated>
  </verify>
  <done>Onboarding wizard flow is functional and mandatory for new users</done>
</task>

<task type="auto">
  <name>Task 2: Profile Management and active_role Toggle</name>
  <files>app/controllers/profiles_controller.rb, app/views/profiles/show.html.erb, app/views/profiles/edit.html.erb, app/models/user.rb</files>
  <action>
    - Set up `Active Storage` for profile pictures.
    - Create `ProfilesController` for viewing and editing name, bio, and avatar (AUTH-05).
    - Implement a role toggle in the UI that updates `active_role` (poster/tasker) and provides visual feedback (D-08).
    - Ensure profile updates respect the user's localized preference.
  </action>
  <verify>
    <automated>grep "active_role" app/models/user.rb && ls app/controllers/profiles_controller.rb</automated>
  </verify>
  <done>Users can manage their profiles and switch between marketplace roles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → application | User profile updates and file uploads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | Profile Form | mitigate | Strong parameters in ProfilesController |
| T-01-03-02 | Information Disclosure | Profile Data | mitigate | Ensure users can only edit their own profile via resource ownership check |
| T-01-03-03 | Elevation of Privilege | active_role | mitigate | Validate that `active_role` only accepts 'poster' or 'tasker' values |
</threat_model>

<verification>
Create a new user, complete the onboarding wizard, verify the role toggle on the profile page, and update profile information.
</verification>

<success_criteria>
- Users are guided through onboarding.
- Roles can be switched easily.
- Profiles are fully manageable and localized.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-03-SUMMARY.md`
</output>
