---
phase: 02-task-marketplace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Gemfile, Gemfile.lock, config/routes.rb, app/controllers/application_controller.rb, app/models/user.rb, app/views/layouts/application.html.erb, app/models/category.rb, db/seeds.rb]
autonomous: true
requirements: [AUTH-03]
must_haves:
  truths:
    - "Navbar is visible on all pages"
    - "User can toggle between Poster and Tasker roles via UI"
    - "At least 5 marketplace categories are seeded in the database"
  artifacts:
    - path: "app/views/shared/_navbar.html.erb"
      provides: "Global navigation and role switcher"
    - path: "app/models/category.rb"
      provides: "Bilingual task categories"
  key_links:
    - from: "app/views/layouts/application.html.erb"
      to: "app/views/shared/_navbar.html.erb"
      via: "render partial"
---

<objective>
Setup the core infrastructure for the Task Marketplace and implement the Role-Switching UI in the navigation.

Purpose: Provide the foundation (gems, authorization, categories) and the entry point for users to choose their role (Poster or Tasker).
Output: Pundit/Geocoder installed, a persistent Navbar with a role switcher, and a Category model with seeded data.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-marketplace/02-RESEARCH.md
@app/models/user.rb
@app/controllers/profiles_controller.rb
@app/views/layouts/application.html.erb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infrastructure Setup (Gems & Pundit)</name>
  <files>Gemfile, config/initializers/geocoder.rb, app/controllers/application_controller.rb</files>
  <action>
    - Add `geocoder`, `pundit`, and `kaminari` gems to Gemfile.
    - Run `bundle install`.
    - Run `rails generate pundit:install`.
    - Run `rails generate geocoder:config`.
    - Include `Pundit::Authorization` in `ApplicationController`.
    - Configure `ApplicationController` to rescue from `Pundit::NotAuthorizedError` by redirecting to root with an alert.
  </action>
  <verify>
    <automated>bundle exec rails generate --help | grep pundit && bundle exec rails generate --help | grep geocoder</automated>
  </verify>
  <done>Gems installed and basic Pundit/Geocoder configuration generated.</done>
</task>

<task type="auto">
  <name>Task 2: Persistent Navbar & Role Switcher UI</name>
  <files>app/views/layouts/application.html.erb, app/views/shared/_navbar.html.erb, app/assets/stylesheets/application.css</files>
  <action>
    - Create `app/views/shared/_navbar.html.erb` with:
      - App Logo/Name (linking to root).
      - Language Switcher (from current layout).
      - Role Switcher Button: Toggle between "Switch to Tasker" and "Switch to Poster" (linked to `toggle_role_profile_path`).
      - User Profile/Logout links.
    - Update `app/views/layouts/application.html.erb` to include the navbar partial at the top.
    - Style the navbar using Tailwind classes for a clean mobile-first design (sticky top, background white, shadow).
    - Ensure the active role is displayed clearly in the UI (e.g., a badge saying "Poster" or "Tasker").
  </action>
  <verify>
    <automated>grep "_navbar" app/views/layouts/application.html.erb && grep "toggle_role" app/views/shared/_navbar.html.erb</automated>
  </verify>
  <done>User can see their current role and toggle it from any page via the navbar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Category Model & Seeds</name>
  <files>app/models/category.rb, db/migrate/[TIMESTAMP]_create_categories.rb, db/seeds.rb</files>
  <behavior>
    - Category should have `name_en` and `name_ne` (bilingual).
    - Validation: `name_en` and `name_ne` must be present and unique.
  </behavior>
  <action>
    - Generate `Category` model with `name_en:string` and `name_ne:string`.
    - Run migrations.
    - Update `db/seeds.rb` to include a standard set of marketplace categories:
      - Plumbing (प्लम्बिङ)
      - Electrical (इलेक्ट्रिकल)
      - Cleaning (सफाई)
      - Delivery (डेलिभरी)
      - Construction (निर्माण)
    - Load seeds: `rails db:seed`.
  </action>
  <verify>
    <automated>bundle exec rails runner "puts Category.count >= 5"</automated>
  </verify>
  <done>Category model exists and is populated with initial bilingual data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Role Switcher | Untrusted user switches their own active role via POST request. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Profile Toggle | mitigate | Validate `active_role` enum values in model and use `current_user` context in controller. |
| T-02-01-02 | Info Disclosure | Categories | accept | Categories are public metadata. |
</threat_model>

<verification>
1. Navbar is visible on all pages.
2. Clicking "Switch to [Role]" changes the user's role and redirects back.
3. Category table has at least 5 entries.
</verification>

<success_criteria>
- User can toggle between 'Poster' and 'Tasker' roles.
- Basic infrastructure (Pundit, Geocoder) is ready for use.
- Standard marketplace categories are available in the database.
</success_criteria>

<output>
After completion, create .planning/phases/02-task-marketplace/02-01-SUMMARY.md
</output>
