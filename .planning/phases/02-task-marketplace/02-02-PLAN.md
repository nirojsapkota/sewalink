---
phase: 02-task-marketplace
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [app/models/task.rb, db/migrate/[TIMESTAMP]_create_tasks.rb, app/controllers/tasks_controller.rb, config/routes.rb, app/views/tasks/new.html.erb, app/views/tasks/index.html.erb, app/policies/task_policy.rb]
autonomous: true
requirements: [TASK-01, TASK-03]
must_haves:
  truths:
    - "Poster can create a task with title, budget, and location"
    - "Task has photos attached via Active Storage"
    - "Task address is geocoded to latitude/longitude automatically"
  artifacts:
    - path: "app/models/task.rb"
      provides: "Task marketplace entity"
    - path: "app/views/tasks/_form.html.erb"
      provides: "Rich task creation interface"
  key_links:
    - from: "app/models/task.rb"
      to: "Active Storage"
      via: "has_many_attached :photos"
    - from: "app/models/task.rb"
      to: "Geocoder"
      via: "geocoded_by :location"
---

<objective>
Implement Task creation and the Poster's dashboard to manage their own tasks.

Purpose: Allow Posters to publish rich task listings with descriptions, photos, budgets, and locations.
Output: Task model with photo attachments and geocoding, a creation form, and a "My Tasks" view for Posters.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-marketplace/02-RESEARCH.md
@app/models/user.rb
@app/models/category.rb
@.planning/phases/02-task-marketplace/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Task Model & Migration</name>
  <files>app/models/task.rb, db/migrate/[TIMESTAMP]_create_tasks.rb</files>
  <behavior>
    - Task belongs to `user` (poster) and `category`.
    - Task has `title`, `description`, `budget`, `location` (address string), `latitude`, `longitude`, and `status` (enum).
    - Status enum: `[:draft, :open, :assigned, :completed, :cancelled]`.
    - Geocode `location` automatically to populate `latitude` and `longitude`.
    - `has_many_attached :photos`.
    - Validations: `title`, `description`, `budget`, `location`, `category_id` must be present.
  </behavior>
  <action>
    - Generate `Task` model with fields: `title:string`, `description:text`, `budget:decimal`, `location:string`, `latitude:float`, `longitude:float`, `status:integer`, `user:references`, `category:references`.
    - Add `enum status: { draft: 0, open: 1, assigned: 2, completed: 3, cancelled: 4 }` to `Task` model.
    - Add `geocoded_by :location` and `after_validation :geocode` to `Task` model.
    - Run migrations.
  </action>
  <verify>
    <automated>bundle exec rails runner "t = Task.new(title: 'Test', description: 'Test', budget: 100, location: 'Kathmandu', status: :open); puts t.valid?"</automated>
  </verify>
  <done>Task model is correctly defined with all required fields and behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Task Posting Flow (Poster)</name>
  <files>app/controllers/tasks_controller.rb, config/routes.rb, app/views/tasks/new.html.erb, app/views/tasks/_form.html.erb, app/policies/task_policy.rb</files>
  <action>
    - Add `resources :tasks` to `config/routes.rb`.
    - Create `TaskPolicy` with Pundit:
      - `create?`: Only if `user.poster?`.
      - `update?`/`destroy?`: Only if `record.user == user`.
    - Implement `TasksController`:
      - `new`: Initialize task with default status `:open`.
      - `create`: Associate with `current_user`, handle strong params including `photos: []`.
    - Create `app/views/tasks/_form.html.erb` with:
      - Category dropdown (from seeded categories).
      - Title, Description, Budget fields.
      - Location field (Text input).
      - Photos upload (Multi-file input).
    - Add Stimulus controller for image previews (refer to RESEARCH.md).
  </action>
  <verify>
    <automated>grep "has_many_attached :photos" app/models/task.rb && grep "TaskPolicy" app/policies/task_policy.rb</automated>
  </verify>
  <done>Posters can successfully create a new task with photos and see validation errors.</done>
</task>

<task type="auto">
  <name>Task 3: Poster's "My Tasks" Dashboard</name>
  <files>app/views/tasks/index.html.erb, app/views/tasks/_task.html.erb</files>
  <action>
    - Update `TasksController#index`: Filter tasks to show only `current_user.tasks` if user is in `poster` role.
    - Create `app/views/tasks/index.html.erb` to list Poster's own tasks.
    - Create `app/views/tasks/_task.html.erb` partial to show task summary (title, status, budget, thumbnail).
    - Add a "Post New Task" button to the index view.
  </action>
  <verify>
    <automated>grep "current_user.tasks" app/controllers/tasks_controller.rb</automated>
  </verify>
  <done>Posters can view and manage their list of posted tasks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Task Creation | Untrusted input (params) for task details and photos. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | Task Ownership | mitigate | Use Pundit to ensure only the creator can update/delete a task. |
| T-02-02-02 | Info Disclosure | Location | accept | Location is intentional public metadata for marketplace. |
</threat_model>

<verification>
1. Poster can access "My Tasks" and click "Post New Task".
2. Filling the form and uploading photos creates a task.
3. Created task shows up in the list with geocoded coordinates (check DB).
</verification>

<success_criteria>
- Poster can create a task with all required metadata and photos.
- Access control prevents Taskers from posting or users from editing others' tasks.
- Task locations are automatically geocoded on save.
</success_criteria>

<output>
After completion, create .planning/phases/02-task-marketplace/02-02-SUMMARY.md
</output>
