---
phase: 02-task-marketplace
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [app/controllers/tasks_controller.rb, app/views/tasks/index.html.erb, app/javascript/controllers/search_controller.js, app/views/tasks/_filter_form.html.erb]
autonomous: true
requirements: [TASK-04]
must_haves:
  truths:
    - "Tasker can see a list of open tasks from other users"
    - "Filtering by category updates the results without page reload"
    - "Searching by location/distance returns tasks within the radius"
  artifacts:
    - path: "app/views/tasks/_filter_form.html.erb"
      provides: "Marketplace search/filter interface"
    - path: "app/javascript/controllers/search_controller.js"
      provides: "Real-time search debouncing"
  key_links:
    - from: "app/controllers/tasks_controller.rb"
      to: "Task.near"
      via: "Geocoder proximity query"
---

<objective>
Implement the Tasker's browsing experience with filters and proximity search.

Purpose: Allow Taskers to find relevant jobs near them or in specific categories.
Output: Integrated search/filter bar with Turbo Frame updates for a snappy browsing experience.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-marketplace/02-RESEARCH.md
@app/models/task.rb
@.planning/phases/02-task-marketplace/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Marketplace Browsing UI (Tasker)</name>
  <files>app/controllers/tasks_controller.rb, app/views/tasks/index.html.erb, app/views/tasks/_task_card.html.erb</files>
  <action>
    - Update `TasksController#index`:
      - If `current_user.tasker?`, show all `:open` tasks.
      - Use `Kaminari` for pagination (`page(params[:page]).per(10)`).
      - Include photos and category to avoid N+1 (`.with_attached_photos.includes(:category)`).
    - Create `app/views/tasks/_task_card.html.erb` for Tasker view (emphasizing budget, distance, and category).
    - Wrap the task list in a `turbo_frame_tag "tasks"` for partial updates.
  </action>
  <verify>
    <automated>grep "turbo_frame_tag \"tasks\"" app/views/tasks/index.html.erb</automated>
  </verify>
  <done>Taskers can view a paginated list of available open tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Live Filtering & Proximity Search</name>
  <files>app/views/tasks/_filter_form.html.erb, app/javascript/controllers/search_controller.js, app/controllers/tasks_controller.rb</files>
  <action>
    - Create `app/views/tasks/_filter_form.html.erb` with:
      - Category filter (Dropdown).
      - Budget range (Min/Max).
      - Proximity search (Distance in km, defaults to 10km).
      - Address/Current Location input.
    - Implement `app/javascript/controllers/search_controller.js` to automatically submit the form on change (debounced).
    - Update `TasksController#index` to apply filters:
      - Category: `where(category_id: params[:category_id])`.
      - Budget: `where(budget: params[:min_budget]..params[:max_budget])`.
      - Proximity: `Task.near(params[:location], params[:distance])` using Geocoder.
    - Ensure results are returned within the `tasks` turbo frame.
  </action>
  <verify>
    <automated>grep "Task.near" app/controllers/tasks_controller.rb</automated>
  </verify>
  <done>Taskers can filter tasks by category, budget, and location with real-time feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Search Filters | Untrusted params used in database queries. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Info Disclosure | Location Privacy | mitigate | Only show approximate locations in list views; never expose exact coordinates of posters until assigned. |
| T-02-03-02 | Denial of Service | Complex Queries | mitigate | Ensure database indexes on `category_id` and `status`. Limit max distance in proximity search. |
</threat_model>

<verification>
1. User in Tasker role can see all open tasks.
2. Selecting a category updates the list without full page reload.
3. Entering a location and distance filters tasks by proximity.
</verification>

<success_criteria>
- Tasker can find tasks using categories, budget, and location filters.
- UI remains responsive during filtering (Turbo Frames).
- Paginated results prevent performance issues.
</success_criteria>

<output>
After completion, create .planning/phases/02-task-marketplace/02-03-SUMMARY.md
</output>
