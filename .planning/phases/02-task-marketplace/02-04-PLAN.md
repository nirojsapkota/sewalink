---
phase: 02-task-marketplace
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified: [app/models/bid.rb, db/migrate/[TIMESTAMP]_create_bids.rb, app/controllers/bids_controller.rb, config/routes.rb, app/views/tasks/show.html.erb, app/views/bids/_form.html.erb, app/policies/bid_policy.rb]
autonomous: true
requirements: [TASK-05]
must_haves:
  truths:
    - "Tasker can submit a bid with price and message"
    - "Poster can see all bids for their own tasks"
    - "Taskers cannot see bids submitted by other taskers"
  artifacts:
    - path: "app/models/bid.rb"
      provides: "Marketplace bid entity"
    - path: "app/views/bids/_form.html.erb"
      provides: "Tasker bidding interface"
  key_links:
    - from: "app/policies/bid_policy.rb"
      to: "Blind Bidding"
      via: "Pundit index/show scopes"
---

<objective>
Implement the Bidding system where Taskers can submit offers on open tasks.

Purpose: Facilitate the negotiation phase of the marketplace loop.
Output: Bid model with price and message, bidding form on Task details page, and blind-bidding logic (Taskers only see their own bids).
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-marketplace/02-RESEARCH.md
@app/models/task.rb
@.planning/phases/02-task-marketplace/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bid Model & Policies</name>
  <files>app/models/bid.rb, db/migrate/[TIMESTAMP]_create_bids.rb, app/policies/bid_policy.rb</files>
  <behavior>
    - Bid belongs to `user` (tasker) and `task`.
    - Bid has `amount` (decimal), `message` (text), and `status` (enum).
    - Status enum: `[:pending, :accepted, :rejected]`.
    - Validations: `amount` must be present and positive, `message` must be present.
    - `BidPolicy`:
      - `create?`: Only if `user.tasker?` and hasn't already bid on the task.
      - `index?`: Only if `record.task.user == user` (Poster) or `record.user == user` (Tasker).
  </behavior>
  <action>
    - Generate `Bid` model with fields: `amount:decimal`, `message:text`, `status:integer`, `user:references`, `task:references`.
    - Add `enum status: { pending: 0, accepted: 1, rejected: 2 }` to `Bid` model.
    - Run migrations.
    - Implement `BidPolicy` with Pundit ensuring blind-bidding (Taskers only see their own).
  </action>
  <verify>
    <automated>bundle exec rails runner "b = Bid.new(amount: 50, message: 'I can help', status: :pending); puts b.valid?"</automated>
  </verify>
  <done>Bid model exists and access control is configured for privacy.</done>
</task>

<task type="auto">
  <name>Task 2: Task Details & Bidding Flow</name>
  <files>app/controllers/tasks_controller.rb, app/views/tasks/show.html.erb, app/controllers/bids_controller.rb, app/views/bids/_form.html.erb, config/routes.rb</files>
  <action>
    - Implement `TasksController#show` to display rich task details (Photos, Category, Budget, Location).
    - Add `resources :bids, only: [:create]` to `config/routes.rb`, nested under `tasks`.
    - On `tasks#show`, if `current_user.tasker?`:
      - Show the `bids/_form.html.erb` if the user hasn't bid.
      - Show their existing bid status if they have.
    - Implement `BidsController#create`:
      - Create bid within a `turbo_stream` response for real-time feedback.
  </action>
  <verify>
    <automated>grep "resources :bids" config/routes.rb</automated>
  </verify>
  <done>Taskers can view task details and submit a bid with amount and message.</done>
</task>

<task type="auto">
  <name>Task 3: Poster Bid Management UI</name>
  <files>app/views/tasks/show.html.erb, app/views/bids/_bid.html.erb</files>
  <action>
    - Update `tasks#show` for Posters:
      - List all received bids using `app/views/bids/_bid.html.erb` partial.
      - Show bidder's name, rating (placeholder for now), amount, and message.
      - Add "Assign Tasker" button next to each bid (form submitting to assignment action in next plan).
  </action>
  <verify>
    <automated>grep "bids.each" app/views/tasks/show.html.erb</automated>
  </verify>
  <done>Posters can review all bids submitted for their own tasks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Bid Submission | Untrusted params for bid amount and message. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Bid Integrity | mitigate | Validate that only Taskers can bid and only once per task. |
| T-02-04-02 | Info Disclosure | Blind Bidding | mitigate | Enforce through Pundit/ActiveRecord scopes that Taskers cannot see others' bids. |
</threat_model>

<verification>
1. Tasker views a task details page.
2. Submitting a bid works and updates the UI via Turbo Streams.
3. Poster views the same task details and sees the bid.
4. Another Tasker views the same task and DOES NOT see the previous Tasker's bid.
</verification>

<success_criteria>
- Tasker can submit exactly one bid per task.
- Poster sees all bids for their task.
- Bids remain private between the Poster and the respective Tasker.
</success_criteria>

<output>
After completion, create .planning/phases/02-task-marketplace/02-04-SUMMARY.md
</output>
