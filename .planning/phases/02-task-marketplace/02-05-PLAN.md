---
phase: 02-task-marketplace
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified: [app/controllers/bids_controller.rb, app/models/task.rb, app/models/bid.rb, app/views/tasks/show.html.erb, app/views/bids/_bid.html.erb, app/controllers/tasker_dashboard_controller.rb]
autonomous: true
requirements: [TASK-06]
must_haves:
  truths:
    - "Poster can accept a bid, changing task status to Assigned"
    - "Accepted bid status changes to Accepted, others to Rejected"
    - "Tasker dashboard shows assigned jobs"
  artifacts:
    - path: "app/controllers/bids_controller.rb"
      provides: "Assignment logic"
    - path: "app/controllers/tasker_dashboard_controller.rb"
      provides: "Tasker tracking UI"
  key_links:
    - from: "app/models/task.rb"
      to: "Turbo Morphing"
      via: "broadcasts_refreshes"
---

<objective>
Implement Task Assignment and complete the core marketplace loop.

Purpose: Allow Posters to select a Tasker for a job, transitioning the task to 'assigned' state.
Output: Assignment logic in controller, real-time UI updates (Turbo 8), and a Tasker dashboard for active jobs.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-marketplace/02-RESEARCH.md
@app/models/task.rb
@app/models/bid.rb
@.planning/phases/02-task-marketplace/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Task Assignment Logic</name>
  <files>app/controllers/bids_controller.rb, app/models/task.rb, app/models/bid.rb</files>
  <action>
    - Add `patch :accept` route to `bids` resource.
    - Implement `BidsController#accept`:
      - Wrap in a transaction:
        - Update `bid.status` to `:accepted`.
        - Update `task.status` to `:assigned`.
        - Mark all other bids for this task as `:rejected`.
    - Add `broadcasts_refreshes` to `Task` and `Bid` models (Turbo 8 Morphing).
  </action>
  <verify>
    <automated>grep "broadcasts_refreshes" app/models/task.rb</automated>
  </verify>
  <done>Poster can assign a tasker, causing state changes across the task and all its bids.</done>
</task>

<task type="auto">
  <name>Task 2: Tasker's "My Bids & Jobs" Dashboard</name>
  <files>app/controllers/tasker_dashboard_controller.rb, app/views/tasker_dashboard/index.html.erb, config/routes.rb</files>
  <action>
    - Create `TaskerDashboardController#index`.
    - Fetch:
      - `current_user.bids.pending`: Active bids.
      - `current_user.bids.accepted.map(&:task)`: Assigned jobs.
    - Create view to list these items.
    - Link to the dashboard from the Navbar (visible only for Taskers).
  </action>
  <verify>
    <automated>grep "tasker_dashboard" config/routes.rb</automated>
  </verify>
  <done>Taskers can track their submitted bids and active assignments in one place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Assignment | Poster selects a bid to accept. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | Assignment State | mitigate | Use database transactions and Pundit to ensure only the task owner can accept a bid. |
| T-02-05-02 | Info Disclosure | Bidder Details | mitigate | Once assigned, share basic contact info (placeholder for now) between Poster and Tasker. |
| T-02-05-03 | Spoofing | Bid acceptance | mitigate | Verify that the accepted bid belongs to the task being assigned. |
</threat_model>

<verification>
1. Poster clicks "Assign Tasker" on a specific bid.
2. Task status changes to "Assigned" and the UI updates (Turbo Morph).
3. The assigned Tasker sees the task in their "Active Jobs" dashboard.
4. Other bidders see their bids as "Rejected".
</verification>

<success_criteria>
- A task can be successfully assigned to exactly one bidder.
- The assignment process is atomic (all status updates or none).
- All participants receive real-time updates via Turbo Morphing.
</success_criteria>

<output>
After completion, create .planning/phases/02-task-marketplace/02-05-SUMMARY.md
</output>
