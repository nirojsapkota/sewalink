---
phase: "03-payments-escrow"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "Gemfile"
  - "config/initializers/money.rb"
  - "config/initializers/double_entry.rb"
  - "app/services/payments/commission_calculator.rb"
autonomous: true
requirements: ["PAY-03"]
must_haves:
  truths:
    - "Currency is correctly handled as NPR with 2-decimal precision."
    - "Immutable ledger accounts exist for Escrow, Tasker Balance, and Platform Revenue."
    - "Commission calculation logic is central and testable."
  artifacts:
    - path: "config/initializers/double_entry.rb"
      provides: "Ledger account definitions"
    - path: "app/services/payments/commission_calculator.rb"
      provides: "Fee calculation (10%)"
  key_links:
    - from: "app/services/payments/commission_calculator.rb"
      to: "money-rails"
      via: "Money objects"
---

<objective>
Establish the financial infrastructure for sewaLink using immutable ledgers and standardized currency handling.

Purpose: Prevent race conditions in balances and ensure an audit trail for every transaction.
Output: Configured double_entry ledger, money-rails NPR setup, and commission calculation service.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-payments-escrow/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install financial dependencies and configure NPR</name>
  <files>Gemfile, config/initializers/money.rb</files>
  <action>
    - Add `double_entry`, `money-rails`, `httparty`, and `aasm` gems to Gemfile.
    - Run `bundle install`.
    - Create `config/initializers/money.rb` and configure it to use NPR (Nepalese Rupee) as the default currency.
    - Set rounding mode to `BigDecimal::ROUND_HALF_UP`.
    - Run `rails generate double_entry:install` to create ledger migrations and run `rails db:migrate`.
  </action>
  <verify>
    <automated>bundle exec rails runner "puts Money.default_currency.iso_code" | grep NPR</automated>
  </verify>
  <done>Gems installed, migrations run, and NPR configured as default currency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define double_entry accounts and transfer rules</name>
  <files>config/initializers/double_entry.rb</files>
  <behavior>
    - Account :escrow (scope: Task, positive_only: true) must exist.
    - Account :tasker_balance (scope: User) must exist (can be negative for CoC).
    - Account :platform_revenue (positive_only: true) must exist.
    - Transfer from :escrow to :tasker_balance (code: :payout) must be allowed.
    - Transfer from :escrow to :platform_revenue (code: :commission) must be allowed.
    - Transfer from :tasker_balance to :platform_revenue (code: :cash_commission) must be allowed.
  </behavior>
  <action>
    Create `config/initializers/double_entry.rb` with the definitions from RESEARCH.md. 
    Ensure `:escrow` is scoped to the `Task` model and `:tasker_balance` is scoped to the `User` model.
  </action>
  <verify>
    <automated>bundle exec rails runner "DoubleEntry.account(:escrow, scope: Task.new)"</automated>
  </verify>
  <done>Ledger accounts and transfer rules are active and verifiable in Rails console.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement CommissionCalculator service</name>
  <files>app/services/payments/commission_calculator.rb</files>
  <behavior>
    - Input: Money object (budget).
    - Output: { total: Money, tasker_share: Money, commission: Money }.
    - Commission should be a flat 10% (inclusive).
    - Ensure correct rounding to 2 decimal places.
  </behavior>
  <action>
    Implement `Payments::CommissionCalculator.call(budget)`. Use `money-rails` for calculations.
    Handle rounding correctly to ensure total = tasker_share + commission.
  </action>
  <verify>
    <automated>bundle exec rspec spec/services/payments/commission_calculator_spec.rb</automated>
  </verify>
  <done>Commission calculation is accurate and handles NPR precision correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Logic -> Ledger | DoubleEntry provides immutable audit trail for internal transfers. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Ledger records | mitigate | Use `double_entry` gem which enforces immutability at the application level. |
| T-03-01-02 | Information Disclosure | Financial Balances | mitigate | Ensure ledger accounts are scoped and access is restricted via Pundit (in later plans). |
</threat_model>

<verification>
- `Money.new(1000, "NPR")` returns correct object.
- `DoubleEntry.account(:escrow, scope: task)` is valid.
- `CommissionCalculator.call(Money.new(1000, "NPR"))[:commission]` returns `Money.new(100, "NPR")`.
</verification>

<success_criteria>
Financial infrastructure is ready to receive and track funds in NPR.
</success_criteria>

<output>
After completion, create .planning/phases/03-payments-escrow/03-01-SUMMARY.md
</output>
