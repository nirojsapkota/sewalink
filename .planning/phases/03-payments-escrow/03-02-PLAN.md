---
phase: "03-payments-escrow"
plan: "02"
type: "execute"
wave: 2
depends_on: ["03-01"]
files_modified:
  - "app/services/payments/esewa_v2.rb"
  - "app/models/payment_transaction.rb"
  - "app/controllers/payments_controller.rb"
  - "config/routes.rb"
autonomous: true
requirements: ["PAY-01"]
must_haves:
  truths:
    - "System can generate a valid eSewa v2 checkout signature."
    - "System verifies payment status via server-to-server API call (never trusting client)."
    - "Payment transaction state is tracked (pending -> completed/failed)."
  artifacts:
    - path: "app/services/payments/esewa_v2.rb"
      provides: "eSewa signature and verification logic"
    - path: "app/models/payment_transaction.rb"
      provides: "Persistence and state tracking for payments"
  key_links:
    - from: "app/controllers/payments_controller.rb"
      to: "eSewa API"
      via: "HTTP POST /api/epay/main/v2/status"
---

<objective>
Integrate eSewa ePay v2 to allow Posters to securely deposit funds.

Purpose: Provide a reliable digital payment gateway while ensuring security through server-side verification.
Output: eSewa v2 service, PaymentTransaction model, and callback handlers.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-payments-escrow/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement EsewaV2 integration service</name>
  <files>app/services/payments/esewa_v2.rb</files>
  <behavior>
    - `EsewaV2.generate_signature(amount, transaction_uuid, product_code)` must match eSewa spec.
    - `EsewaV2.verify_payment(transaction_uuid, amount)` must call eSewa API and return status.
    - Correct environment URLs (Sandbox vs Production) must be handled via ENV.
  </behavior>
  <action>
    Create `app/services/payments/esewa_v2.rb` based on the RESEARCH.md example.
    Implement HMAC-SHA256 signing and status check using `httparty`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/services/payments/esewa_v2_spec.rb</automated>
  </verify>
  <done>eSewa v2 integration service is robust and handles signature correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Create PaymentTransaction model and checkout flow</name>
  <files>app/models/payment_transaction.rb, app/controllers/payments_controller.rb, config/routes.rb</files>
  <action>
    - Generate `PaymentTransaction` model with fields: `task_id`, `amount_cents`, `transaction_uuid`, `status` (enum: pending, completed, failed), `external_ref`.
    - Use `AASM` for status transitions.
    - Implement `PaymentsController#create` to initialize a transaction and return the eSewa form data.
    - Define routes for `payments/create`, `payments/success`, `payments/failure`.
  </action>
  <verify>
    <automated>bundle exec rails runner "PaymentTransaction.create!(task: Task.first, amount_cents: 10000)"</automated>
  </verify>
  <done>Payment tracking model exists and checkout controller is initialized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement secure success and webhook callbacks</name>
  <files>app/controllers/payments_controller.rb</files>
  <behavior>
    - `success` callback must perform server-to-server verification.
    - Transaction must only be marked `completed` if the verification API returns success.
    - Handle duplicate callbacks gracefully (idempotency).
  </behavior>
  <action>
    Implement logic in `PaymentsController#success` to call `EsewaV2.verify_payment`.
    Update the `PaymentTransaction` status based on the result.
    Redirect the user to the Task page with a success/failure toast.
  </action>
  <verify>
    <automated>bundle exec rspec spec/requests/payments_controller_spec.rb</automated>
  </verify>
  <done>Payment callbacks are secure and server-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| eSewa -> App | External redirect/callback. NEVER trust params; always verify via API. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing | Payment callback | mitigate | Mandatory server-to-server status check via eSewa REST API. |
| T-03-02-02 | Tampering | Transaction ID | mitigate | Use UUIDs for `transaction_uuid` and verify amounts match the record. |
</threat_model>

<verification>
- eSewa signature generation passes against known test vectors.
- `PaymentsController#success` rejects spoofed params without an API hit.
- `PaymentTransaction` state moves to `completed` after verified payment.
</verification>

<success_criteria>
Posters can reach the eSewa payment page and the platform securely records the outcome.
</success_criteria>

<output>
After completion, create .planning/phases/03-payments-escrow/03-02-SUMMARY.md
</output>
