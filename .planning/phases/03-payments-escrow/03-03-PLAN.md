---
phase: "03-payments-escrow"
plan: "03"
type: "execute"
wave: 3
depends_on: ["03-02"]
files_modified:
  - "app/services/payments/ledger_manager.rb"
  - "app/models/task.rb"
  - "app/controllers/tasks/completion_controller.rb"
autonomous: true
requirements: ["PAY-01", "PAY-03"]
must_haves:
  truths:
    - "Verified payment automatically triggers funds deposit into Escrow ledger."
    - "Completing a task automatically triggers Escrow release to Tasker and Revenue."
    - "Task completion is blocked if funds are not yet in Escrow (for digital tasks)."
  artifacts:
    - path: "app/services/payments/ledger_manager.rb"
      provides: "Abstraction for all Ledger transfers"
    - path: "app/models/task.rb"
      provides: "Escrow-aware lifecycle transitions"
  key_links:
    - from: "PaymentTransaction"
      to: "LedgerManager"
      via: "deposit_to_escrow(task)"
    - from: "Task"
      to: "LedgerManager"
      via: "release_escrow(task)"
---

<objective>
Implement the core Escrow lifecycle: moving funds from Payment to Escrow, and then from Escrow to the Tasker's balance upon completion.

Purpose: Automate the financial side of task completion and ensure the platform collects its commission.
Output: LedgerManager service and escrow-aware Task lifecycle.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-payments-escrow/03-RESEARCH.md
@.planning/phases/03-payments-escrow/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement LedgerManager for Escrow operations</name>
  <files>app/services/payments/ledger_manager.rb</files>
  <behavior>
    - `deposit_to_escrow(task)`: Moves budget from :user_external to :escrow (scoped to task).
    - `release_from_escrow(task)`: Moves funds from :escrow to :tasker_balance and :platform_revenue (split via CommissionCalculator).
    - Ensure all operations are wrapped in DB transactions.
  </behavior>
  <action>
    Implement `Payments::LedgerManager`. Use `DoubleEntry.transfer` as defined in the initializer.
    Integrate `CommissionCalculator` in the release logic.
  </action>
  <verify>
    <automated>bundle exec rspec spec/services/payments/ledger_manager_spec.rb</automated>
  </verify>
  <done>Escrow deposit and release logic is functional and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Escrow to Task lifecycle</name>
  <files>app/models/task.rb, app/models/payment_transaction.rb</files>
  <action>
    - Add `after_commit` hook on `PaymentTransaction` (when status moves to `completed`) to call `LedgerManager.deposit_to_escrow`.
    - Update `Task` model to track `payment_type` (enum: esewa, cash).
    - Add validation to `Task` to ensure digital tasks have a verified deposit before moving to `in_progress`.
  </action>
  <verify>
    <automated>bundle exec rails runner "t = Task.first; pt = PaymentTransaction.create!(task: t, status: :completed); t.reload; puts pt.status"</automated>
  </verify>
  <done>Escrow deposit is automatically triggered by successful payment.</done>
</task>

<task type="auto">
  <name>Task 3: Implement automatic Escrow release on completion</name>
  <files>app/controllers/tasks/completion_controller.rb, app/models/task.rb</files>
  <action>
    - Update the Task completion logic (where Poster marks as 'Done') to call `LedgerManager.release_from_escrow` if `payment_type` is `esewa`.
    - Ensure funds are only released ONCE (idempotency in LedgerManager).
    - Provide visual feedback to the Poster that payment has been released.
  </action>
  <verify>
    <automated>bundle exec rails runner "t = Task.first; t.update!(status: :completed); puts t.status"</automated>
  </verify>
  <done>Funds move from Escrow to Tasker's balance when the job is confirmed finished.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Logic -> Ledger | Core financial state changes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | Escrow Balance | mitigate | All transfers occur through `LedgerManager` with audit trails in `double_entry_lines`. |
| T-03-03-02 | Denial of Service | Deadlocks | mitigate | Wrap ledger transfers in short-lived database transactions. |
</threat_model>

<verification>
- `Task#completed` (digital) triggers `DoubleEntry.account(:escrow, scope: task)` balance -> 0.
- Tasker balance increases by (Budget - Commission).
- Platform Revenue increases by Commission.
</verification>

<success_criteria>
Full digital payment loop: Deposit -> Escrow -> Release -> Commission is automated and correct.
</success_criteria>

<output>
After completion, create .planning/phases/03-payments-escrow/03-03-SUMMARY.md
</output>
