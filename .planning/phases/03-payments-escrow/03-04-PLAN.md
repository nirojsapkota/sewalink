---
phase: "03-payments-escrow"
plan: "04"
type: "execute"
wave: 2
depends_on: ["03-01"]
files_modified:
  - "app/models/bid.rb"
  - "app/views/tasks/_bid_form.html.erb"
  - "app/services/payments/ledger_manager.rb"
  - "app/models/user.rb"
autonomous: true
requirements: ["PAY-02", "PAY-03"]
must_haves:
  truths:
    - "Poster and Tasker can agree on a Cash-on-Completion (CoC) payment method."
    - "Completing a CoC task debits the platform commission from the Tasker's balance (allowing negative balance)."
    - "Taskers with balance below -500 NPR are blocked from placing new bids."
  artifacts:
    - path: "app/models/user.rb"
      provides: "Balance check logic (negative balance threshold)"
    - path: "app/services/payments/ledger_manager.rb"
      provides: "CoC commission record logic"
  key_links:
    - from: "Task#complete"
      to: "LedgerManager#record_cash_commission"
      via: "Negative balance debit"
---

<objective>
Enable Cash-on-Completion (CoC) as a valid payment option while ensuring the platform still collects its commission through a debt-tracking system.

Purpose: Facilitate localized on-site payments and reduce digital friction while maintaining platform monetization.
Output: CoC bidding options, negative balance commission logic, and bidding safeguards.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-payments-escrow/03-RESEARCH.md
@.planning/phases/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Payment Method selection to Bidding flow</name>
  <files>app/models/bid.rb, app/views/tasks/_bid_form.html.erb, app/models/task.rb</files>
  <action>
    - Update `Bid` model to include `payment_method` (enum: esewa, cash).
    - Update `Bid` form to allow Taskers to propose their preferred payment method.
    - Update `Task` model to store the finalized `payment_type` when a bid is accepted.
  </action>
  <verify>
    <automated>bundle exec rails runner "Bid.create!(task: Task.first, user: User.last, amount: 1000, payment_method: :cash)"</automated>
  </verify>
  <done>Taskers can specify their preferred payment method during bidding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Cash Commission recording in LedgerManager</name>
  <files>app/services/payments/ledger_manager.rb</files>
  <behavior>
    - `record_cash_commission(task)`: Moves commission amount from `:tasker_balance` (scoped to User) to `:platform_revenue`.
    - This creates a negative balance in `:tasker_balance` (debt to platform).
  </behavior>
  <action>
    Implement `Payments::LedgerManager.record_cash_commission(task)`.
    Deduct the commission from the Tasker's ledger balance using the `:cash_commission` transfer code.
    Wire this to run when a Task with `payment_type: :cash` is marked as 'Done'.
  </action>
  <verify>
    <automated>bundle exec rspec spec/services/payments/ledger_manager_coc_spec.rb</automated>
  </verify>
  <done>Cash tasks correctly record platform commission as a debt on the Tasker's account.</done>
</task>

<task type="auto">
  <name>Task 3: Implement BiddingGuard for negative balances</name>
  <files>app/models/user.rb, app/models/bid.rb</files>
  <action>
    - Add `User#can_bid?` method that checks if `balance > -500 NPR`.
    - Add model validation to `Bid` to prevent creation if `user.can_bid?` is false.
    - Display a clear warning in the UI if a Tasker is blocked due to high debt.
  </action>
  <verify>
    <automated>bundle exec rails runner "u = User.first; DoubleEntry.transfer(Money.new(60000, 'NPR'), from: :tasker_balance, to: :platform_revenue, code: :cash_commission, scope: u); puts u.can_bid?" | grep false</automated>
  </verify>
  <done>System prevents Taskers with high platform debt from placing new bids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Logic -> Ledger | Core financial state changes for debt. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Debt evasion | mitigate | Automated bidding block for negative balances below the threshold. |
| T-03-04-02 | Repudiation | Cash completion | mitigate | Both Poster and Tasker must confirm task completion in later phases (Phase 5). For now, Poster confirmation triggers commission. |
</threat_model>

<verification>
- Tasker balance becomes negative after completing a Cash task.
- Platform revenue increases after completing a Cash task.
- Tasker with -600 NPR balance cannot create a new Bid.
</verification>

<success_criteria>
Platform monetization is secured for both digital and cash-based transactions.
</success_criteria>

<output>
After completion, create .planning/phases/03-payments-escrow/03-04-SUMMARY.md
</output>
