---
phase: "03-payments-escrow"
plan: "05"
type: "execute"
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - "app/models/payout_request.rb"
  - "app/controllers/tasker/wallets_controller.rb"
  - "app/views/tasker/wallets/show.html.erb"
  - "app/controllers/admin/payouts_controller.rb"
  - "config/routes.rb"
autonomous: true
requirements: ["PAY-04"]
must_haves:
  truths:
    - "Tasker can see their current ledger balance and transaction history in a dedicated 'Wallet' view."
    - "Tasker can submit a request for a payout (NPR 500 minimum)."
    - "Payout requests are tracked in states (pending, processed, rejected)."
  artifacts:
    - path: "app/models/payout_request.rb"
      provides: "Persistence and state management for withdrawals"
    - path: "app/controllers/tasker/wallets_controller.rb"
      provides: "Wallet UI logic"
  key_links:
    - from: "DoubleEntry::Line"
      to: "Wallet UI"
      via: "Historical transaction rendering"
---

<objective>
Implement the Tasker Wallet and Payout request system, giving Taskers control over their earnings.

Purpose: Complete the marketplace financial loop by allowing users to withdraw their platform-held funds.
Output: PayoutRequest model, Wallet UI, and basic admin payout management.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-payments-escrow/03-RESEARCH.md
@.planning/phases/03-payments-escrow/03-03-SUMMARY.md
@.planning/phases/03-payments-escrow/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PayoutRequest model and workflow</name>
  <files>app/models/payout_request.rb, config/routes.rb</files>
  <action>
    - Generate `PayoutRequest` model with fields: `user_id`, `amount_cents`, `status` (pending, processed, rejected), `payment_details` (text), `rejection_reason` (text).
    - Use `AASM` for status transitions.
    - Add validation for `amount_cents >= 50000` (NPR 500).
    - Add validation to ensure `user.balance >= payout_request.amount`.
  </action>
  <verify>
    <automated>bundle exec rails runner "PayoutRequest.create!(user: User.first, amount_cents: 100000, status: :pending)"</automated>
  </verify>
  <done>PayoutRequest model and workflow are defined.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Tasker Wallet view</name>
  <files>app/controllers/tasker/wallets_controller.rb, app/views/tasker/wallets/show.html.erb</files>
  <action>
    - Create `Tasker::WalletsController#show` to fetch the current User's ledger balance and `DoubleEntry::Line` history.
    - Implement the Wallet view showing: Total Balance, Earnings, Pending Bids (potential income), and a list of transactions.
    - Include a simple "Request Payout" form in the Wallet view.
  </action>
  <verify>
    <automated>ls app/views/tasker/wallets/show.html.erb</automated>
  </verify>
  <done>Tasker can view their balance and historical transaction log from the ledger.</done>
</task>

<task type="auto">
  <name>Task 3: Implement basic Admin Payout management</name>
  <files>app/controllers/admin/payouts_controller.rb, app/views/admin/payouts/index.html.erb</files>
  <action>
    - Create an admin-only view to list all `pending` PayoutRequests.
    - Implement "Mark as Processed" action which: 
      1. Moves the PayoutRequest to `processed`.
      2. Debits the `:tasker_balance` (payout amount) to `:user_external`. (Transfer code: :payout).
  </action>
  <verify>
    <automated>bundle exec rails runner "pr = PayoutRequest.first; pr.process!; puts pr.status" | grep processed</automated>
  </verify>
  <done>Admins can process withdrawal requests and update the ledger accordingly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> Admin | Payout request vetting. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | Payout amount | mitigate | Model-level validation ensures requested amount does not exceed ledger balance. |
| T-03-05-02 | Elevation of Privilege | Payout processing | mitigate | `Admin::PayoutsController` must be protected by Pundit/Devise admin check. |
</threat_model>

<verification>
- Tasker wallet shows accurate balance based on `double_entry`.
- Payout request fails if balance is insufficient.
- Admin processing a payout correctly debits the Tasker ledger.
</verification>

<success_criteria>
Taskers have a clear view of their earnings and a reliable way to receive payouts.
</success_criteria>

<output>
After completion, create .planning/phases/03-payments-escrow/03-05-SUMMARY.md
</output>
