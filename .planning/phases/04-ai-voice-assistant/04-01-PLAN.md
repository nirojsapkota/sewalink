---
phase: 04-ai-voice-assistant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["Gemfile", "config/initializers/openai.rb", "app/services/task_draft_generator_service.rb", "spec/services/task_draft_generator_service_spec.rb"]
autonomous: true
requirements: ["TASK-02"]
user_setup:
  - service: openai
    why: "Transcription and text analysis for Magic Box"
    env_vars:
      - name: OPENAI_ACCESS_TOKEN
        source: "OpenAI Dashboard -> API Keys"

must_haves:
  truths:
    - "System can connect to OpenAI API"
    - "System can send an audio file for transcription and parse details"
  artifacts:
    - path: "app/services/task_draft_generator_service.rb"
      provides: "Audio transcription and GPT parsing service"
      exports: ["call"]
  key_links:
    - from: "app/services/task_draft_generator_service.rb"
      to: "OpenAI::Client"
      via: "API calls to Whisper and GPT-4o-mini"
---

<objective>
Setup OpenAI integration and create a backend service capable of transforming audio recordings into structured Task data.

Purpose: To provide the backend intelligence for the "Magic Box" voice-to-task creation feature, lowering the barrier to entry.
Output: Configured ruby-openai gem and `TaskDraftGeneratorService`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ruby-openai gem and configure</name>
  <files>Gemfile, config/initializers/openai.rb</files>
  <action>
    Add the `ruby-openai` gem to the Gemfile and run bundle install.
    Create `config/initializers/openai.rb` to initialize the client using `ENV.fetch("OPENAI_ACCESS_TOKEN", nil)`.
    Ensure the initializer uses `nil` fallback so the app can boot without the key in environments where voice isn't being tested.
  </action>
  <verify>
    <automated>bundle exec rails runner "OpenAI::Client.new"</automated>
  </verify>
  <done>ruby-openai gem installed and initialized successfully</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TaskDraftGeneratorService</name>
  <files>app/services/task_draft_generator_service.rb, spec/services/task_draft_generator_service_spec.rb</files>
  <behavior>
    - Test 1: Mocks OpenAI transcription (returning "I need a plumber to fix a pipe for 500 rupees") and GPT extraction, expecting a hash with title, description, budget=500, category_id.
    - Test 2: Handles OpenAI API errors gracefully, returning an error object or hash.
  </behavior>
  <action>
    Create a service class `TaskDraftGeneratorService` initialized with a path to an audio file.
    It should have a `#call` method that performs two steps:
    1. Uses `OpenAI::Client.new.audio.transcribe` with the whisper-1 model on the given file path to get a transcript.
    2. Uses `OpenAI::Client.new.chat` with `gpt-4o-mini` and `response_format: { type: "json_object" }` to extract `title`, `description`, `budget` (as integer), and infer `category_id` based on a list of categories mapped from the DB.
    Return a structured JSON/Hash with these extracted keys.
  </action>
  <verify>
    <automated>bundle exec rspec spec/services/task_draft_generator_service_spec.rb</automated>
  </verify>
  <done>Service successfully takes audio, hits OpenAI, and returns a structured hash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system -> OpenAI API | Sending untrusted audio file bytes to OpenAI servers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | TaskDraftGeneratorService | mitigate | Ensure strict file extension and MIME type validation before sending to OpenAI. Limit file size (e.g., max 10MB) to prevent DoS. |
| T-04-02 | Info Disclosure | OpenAI API Integration | mitigate | Use ENV variables for the API key, never hardcode. Don't log full transcriptions if they contain PII. |
</threat_model>

<verification>
- `ruby-openai` is installed and loadable
- `TaskDraftGeneratorService` tests pass mocking OpenAI calls
</verification>

<success_criteria>
Service exists that turns an audio file path into a structured JSON payload containing title, description, budget, and category_id.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-voice-assistant/04-01-SUMMARY.md`
</output>
