---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: ["05-00"]
files_modified: [
  "app/models/task.rb",
  "spec/models/task_spec.rb",
  "db/migrate/20260416000001_add_geofencing_to_tasks.rb"
]
autonomous: true
requirements: [SAFE-01, SAFE-04]
user_setup: []

must_haves:
  truths:
    - "Task model has on_site boolean field (default: true)"
    - "AASM transitions include geofence and completion photo guards"
  artifacts:
    - path: "app/models/task.rb"
      provides: "AASM transitions with geofence guards"
    - path: "spec/models/task_spec.rb"
      provides: "Verification of geofencing model logic"
---

<objective>
Implement geofencing model logic and database schema changes for physical presence verification.

Purpose: Establishes the server-side enforcement for on-site task completion.
Output: Geofenced task lifecycle in the model with AASM guards.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trust-safety-support/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Geofencing fields and Mandatory Completion Photo</name>
  <files>app/models/task.rb, db/migrate/20260416000001_add_geofencing_to_tasks.rb, spec/models/task_spec.rb</files>
  <read_first>
    - app/models/task.rb
    - spec/models/task_spec.rb
  </read_first>
  <action>
    - Create migration adding `on_site:boolean, default: true` to `tasks`.
    - Update `Task` model to include `has_one_attached :completion_photo`.
    - Enhance AASM state machine in `Task`:
      - Add `within_geofence?` guard using `geocoder` gem (`distance_from`).
      - Add `completion_photo_attached?` guard for `complete` event.
      - Implement `check_in!` method to transition to `in_progress` if `within_geofence?`.
    - Update `spec/models/task_spec.rb` with tests for:
      - `within_geofence?` logic.
      - `check_in!` transition.
      - Completion block if outside geofence or photo missing.
    - Per D-01, D-03, D-09.
  </action>
  <done>Model logic and schema changes are defined with tests.</done>
  <verify>
    <automated>ls db/migrate/*_add_geofencing_to_tasks.rb</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Run migrations and verify model logic</name>
  <files>db/schema.rb</files>
  <action>Run bin/rails db:migrate to apply geofencing fields and verify Task model with RSpec.</action>
  <verify>
    <automated>bin/rails db:migrate && bundle exec rspec spec/models/task_spec.rb</automated>
  </verify>
  <done>Database is updated and model logic is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client Geolocation → Server | Model-level verification ensures server-side enforcement. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Task AASM Guards | mitigate | Server-side distance check using Geocoder gem; guards prevent status transitions without meeting criteria. |
</threat_model>

<verification>
Run `bundle exec rspec spec/models/task_spec.rb`.
</verification>

<success_criteria>
- Geofencing logic exists in the model.
- Mandatory photo requirement is enforced at the model level.
- Tests pass for geofence guards.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trust-safety-support/05-01-SUMMARY.md`
</output>
