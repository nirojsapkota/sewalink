---
phase: "05"
plan: "01.1"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [
  "app/javascript/controllers/geolocation_controller.js",
  "app/javascript/controllers/index.js",
  "app/views/tasks/show.html.erb",
  "app/controllers/tasks_controller.rb"
]
autonomous: false
requirements: [SAFE-01]
user_setup: []

must_haves:
  truths:
    - "Tasker status automatically transitions to 'in_progress' when within 200m of the task"
    - "Tasker is blocked from marking task as 'Done' if distance > 200m from task coordinates"
  artifacts:
    - path: "app/javascript/controllers/geolocation_controller.js"
      provides: "Client-side location tracking and server sync"
---

<objective>
Implement the geolocation UI and Stimulus controller for real-time geofence enforcement.

Purpose: Automates check-in and provides immediate feedback to the tasker on-site.
Output: Geolocation Stimulus controller and UI status indicators.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trust-safety-support/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Geolocation Stimulus Controller and Auto-Check-in</name>
  <files>app/javascript/controllers/geolocation_controller.js, app/javascript/controllers/index.js, app/views/tasks/show.html.erb, app/controllers/tasks_controller.rb</files>
  <read_first>
    - app/javascript/controllers/index.js
    - app/views/tasks/show.html.erb
  </read_first>
  <action>
    - Create `geolocation_controller.js` using `navigator.geolocation.getCurrentPosition`.
    - Implement `checkGeofence` function that sends coordinates to the server via Turbo/Fetch.
    - Update `TasksController#update` to handle coordinate parameters and trigger `check_in!` if applicable (D-02).
    - Add geofence status indicator and "Mark as Done" conditional rendering to task view.
    - Use `enableHighAccuracy: true`.
  </action>
  <verify>
    <automated>ls app/javascript/controllers/geolocation_controller.js</automated>
  </verify>
  <done>Client-side location tracking is integrated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Geofencing Flow</name>
  <files>app/javascript/controllers/geolocation_controller.js</files>
  <action>
    Manual verification of geofence enforcement.
    1. Create a task with a specific address.
    2. Assign yourself as a tasker.
    3. Open the task view.
    4. Use browser location simulation (DevTools -> Sensors) to set coordinates > 200m away.
    5. Verify "Mark as Done" is disabled.
    6. Simulate coordinates within 200m.
    7. Verify status auto-transitions to "In Progress".
    8. Attach a photo and verify "Mark as Done" works.
  </action>
  <verify>
    <automated>echo "Manual verification required"</automated>
  </verify>
  <done>Geofencing flow is verified by human.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client Geolocation → Server | Client sends coordinates to server for verification. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01b| Spoofing | Geolocation JS | accept | GPS spoofing is possible but mitigated by mandatory completion photo (D-09) and server-side model-level guards. |
</threat_model>

<success_criteria>
- Auto-check-in works when tasker is near location.
- "Mark as Done" reflects geofence status in UI.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trust-safety-support/05-01.1-SUMMARY.md`
</output>
