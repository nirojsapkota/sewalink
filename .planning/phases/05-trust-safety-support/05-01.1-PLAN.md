---
phase: "05"
plan: "01.1"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [
  "app/javascript/controllers/geolocation_controller.js",
  "app/javascript/controllers/index.js",
  "app/views/tasks/show.html.erb",
  "app/controllers/tasks_controller.rb"
]
autonomous: false
requirements: [SAFE-01]
user_setup: []

must_haves:
  truths:
    - "Tasker can click a 'Check-in' button to transition task to 'in_progress' only when within 200m of the task"
    - "Tasker is blocked from marking task as 'Done' if distance > 200m from task coordinates"
    - "Geolocation API calls are limited to user-initiated actions (e.g., button click) or a low-frequency continuous check for UI status."
  artifacts:
    - path: "app/javascript/controllers/geolocation_controller.js"
      provides: "Client-side location tracking and server sync for check-in button"
---

<objective>
Implement the geolocation UI and Stimulus controller for user-initiated check-in and real-time geofence enforcement. The continuous geofence checks are removed to limit API calls, replaced by a "Check-in" button.

Purpose: Automates check-in only when explicitly requested by the tasker via a button, and provides immediate geofence feedback for 'Mark as Done'.
Output: Geolocation Stimulus controller, a 'Check-in' button, and UI status indicators.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trust-safety-support/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Geolocation Stimulus Controller for Manual Check-in</name>
  <files>app/javascript/controllers/geolocation_controller.js, app/javascript/controllers/index.js, app/views/tasks/show.html.erb, app/controllers/tasks_controller.rb</files>
  <read_first>
    - app/javascript/controllers/index.js
    - app/views/tasks/show.html.erb
  </read_first>
  <action>
    - **Modify `TasksController#check_geofence`**:
      - Remove `if within_geofence && @task.may_start_work?` and `@task.check_in!` logic.
      - It should only return `within_geofence` status.
    - **Add `TasksController#perform_check_in` action**:
      - This new action will receive current coordinates.
      - It will call `@task.check_in!` only if `within_geofence?` is true.
      - Handle `AASM::InvalidTransition`.
    - **Modify `geolocation_controller.js`**:
      - Change `startWatchingLocation` to only update UI status, not trigger `checkGeofence` automatically. The continuous `watchPosition` should be removed. Instead, a `getCurrentPosition` call should be made when the "Check-in" button is active or when explicitly requested.
      - Keep `checkGeofence` method, but it should be callable on demand (e.g., via a Stimulus action).
      - Add a new Stimulus action (e.g., `checkIn`) that makes a POST request to `TasksController#perform_check_in` with current coordinates.
    - **Modify `app/views/tasks/show.html.erb`**:
      - Add a "Check-in" button.
      - The button should call the `geolocation` controller's `checkIn` action.
      - This button should only be enabled when the current task status is `assigned` and the geofence check indicates the user is within range.

    - **Ensure `checkGeofence` is only called:**
        - When the page loads initially (to set button state).
        - When the 'Check-in' button is clicked.
        - When the 'Mark as Done' button is enabled/disabled (can reuse the `checkGeofence` function, but it won't trigger `check_in!`).
  </action>
  <verify>
    <automated>ls app/javascript/controllers/geolocation_controller.js</automated>
  </verify>
  <done>User-initiated check-in functionality is integrated and geofence calls are optimized.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Geofencing Flow with Manual Check-in</name>
  <files>app/javascript/controllers/geolocation_controller.js</files>
  <action>
    Manual verification of geofence enforcement with user-initiated check-in.
    1. Create a task with a specific address.
    2. Assign yourself as a tasker.
    3. Open the task view.
    4. Simulate coordinates > 200m away.
    5. Verify "Check-in" button is disabled.
    6. Simulate coordinates within 200m.
    7. Verify "Check-in" button is enabled. Click it.
    8. Verify task status transitions to "In Progress".
    9. Verify "Mark as Done" button's state (enabled/disabled) based on geofence.
    10. Attach a photo and verify "Mark as Done" works and completes the task.
  </action>
  <verify>
    <automated>echo "Manual verification required"</automated>
  </verify>
  <done>Geofencing flow with manual check-in is verified by human.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client Geolocation → Server | Client sends coordinates to server for verification. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01b| Spoofing | Geolocation JS | accept | GPS spoofing is possible but mitigated by mandatory completion photo (D-09) and server-side model-level guards. |
</threat_model>

<success_criteria>
- User-initiated check-in works when tasker is near location.
- "Mark as Done" reflects geofence status in UI.
- Geolocation API calls are significantly reduced compared to continuous watching.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trust-safety-support/05-01.1-SUMMARY.md`
</output>
