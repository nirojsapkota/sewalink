---
phase: "05"
plan: "02"
type: execute
wave: 1
depends_on: ["05-00"]
files_modified: [
  "app/models/review.rb",
  "app/controllers/reviews_controller.rb",
  "app/jobs/close_review_window_job.rb",
  "db/migrate/20260416000002_create_reviews.rb",
  "spec/models/review_spec.rb"
]
autonomous: true
requirements: [SAFE-02]
user_setup: []

must_haves:
  truths:
    - "Poster can leave a public review and rating for a Tasker"
    - "Tasker can leave a private note for Admin about a Poster"
    - "Reviews are hidden (blind) until both parties submit or 14 days pass"
    - "Review window closes automatically after 14 days"
  artifacts:
    - path: "app/models/review.rb"
      provides: "Blind review logic and visibility rules"
    - path: "app/jobs/close_review_window_job.rb"
      provides: "Automated review publishing after 14 days"
    - path: "spec/models/review_spec.rb"
      provides: "Verification of review blind logic"
---

<objective>
Implement a blind review system to establish social trust while preventing retaliatory feedback.

Purpose: Ensures honest feedback by hiding reviews until both parties have contributed.
Output: Review model, blind visibility logic, and background jobs.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trust-safety-support/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Review model and migrations</name>
  <files>app/models/review.rb, db/migrate/20260416000002_create_reviews.rb</files>
  <read_first>
    - db/schema.rb
    - app/models/task.rb
  </read_first>
  <action>
    - Create migration for `reviews` table:
      - `task_id:bigint` (references tasks)
      - `reviewer_id:bigint` (references users)
      - `reviewee_id:bigint` (references users)
      - `rating:integer`
      - `comment:text`
      - `private_note:text`
      - `is_public:boolean, default: false`
    - Add associations to `Task` and `User` models.
    - Implement validation: `rating` (1-5), `comment` presence if public.
  </action>
  <verify>
    <automated>ls db/migrate/*_create_reviews.rb</automated>
  </verify>
  <done>Review infrastructure is defined.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Run migrations and implement Blind Logic</name>
  <files>db/schema.rb, app/models/review.rb, app/jobs/close_review_window_job.rb, spec/models/review_spec.rb</files>
  <read_first>
    - app/models/review.rb
    - spec/models/review_spec.rb
  </read_first>
  <action>
    - Run bin/rails db:migrate.
    - Implement `visible?` logic in `Review`:
      - True if both `Task` reviews (one from each party) are present OR `created_at > 14.days`.
    - Create `CloseReviewWindowJob` that runs 14 days after task completion.
    - Update `spec/models/review_spec.rb` to test:
      - Visibility rules (blind logic).
      - Rating validations.
      - Association constraints.
    - Per D-07, D-08.
  </action>
  <verify>
    <automated>bin/rails db:migrate && bundle exec rspec spec/models/review_spec.rb</automated>
  </verify>
  <done>Blind review system is active and verified.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Review UI and Forms</name>
  <files>app/controllers/reviews_controller.rb, app/views/reviews/_form.html.erb, app/views/tasks/show.html.erb</files>
  <read_first>
    - app/views/tasks/show.html.erb
  </read_first>
  <action>
    - Create `ReviewsController#create`.
    - Implement review submission form in Task show page (only visible after completion).
    - Add logic to show "Review submitted" state.
    - Ensure Tasker reviews for Posters stay private in UI (D-06).
    - Per D-05.
  </action>
  <verify>
    <automated>ls app/controllers/reviews_controller.rb</automated>
  </verify>
  <done>Review UI is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Public Profiles → Review Data | Reviews should only be exposed when visibility conditions are met. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | Review#visible? | mitigate | Strict model-level scoping; reviews only returned in queries if `visible?` or owned by requester. |
</threat_model>

<verification>
Run `bundle exec rspec spec/models/review_spec.rb`.
</verification>

<success_criteria>
- Review creation works for both roles.
- Blind logic prevents early visibility.
- 14-day window enforcement is scheduled.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trust-safety-support/05-02-SUMMARY.md`
</output>
