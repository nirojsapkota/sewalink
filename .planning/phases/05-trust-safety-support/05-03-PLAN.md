---
phase: "05"
plan: "03"
type: execute
wave: 2
depends_on: ["05-01", "05-00"]
files_modified: [
  "app/models/conversation.rb",
  "app/models/message.rb",
  "app/services/content_filter_service.rb",
  "db/migrate/20260416000003_create_conversations_and_messages.rb",
  "spec/models/message_spec.rb",
  "spec/services/content_filter_service_spec.rb",
  "app/policies/conversation_policy.rb",
  "spec/policies/conversation_policy_spec.rb"
]
autonomous: true
requirements: [SAFE-05, SAFE-06]
user_setup: []

must_haves:
  truths:
    - "Each Bid automatically initializes a Conversation thread"
    - "Messages mask Nepal phone numbers and email addresses until task is assigned"
    - "Conversations are archived/hidden for non-assigned bidders after assignment"
  artifacts:
    - path: "app/models/conversation.rb"
      provides: "Bid-linked messaging container"
    - path: "app/services/content_filter_service.rb"
      provides: "Regex-based PII masking"
    - path: "spec/services/content_filter_service_spec.rb"
      provides: "Validation of Nepal-specific PII regex"
---

<objective>
Establish secure communication infrastructure with automated content filtering to protect user privacy before commitment.

Purpose: Prevents premature exchange of contact info and organizes chat by task context.
Output: Messaging models, PII filter service, and bid integration.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trust-safety-support/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Messaging models and Bid integration</name>
  <files>app/models/conversation.rb, app/models/message.rb, db/migrate/20260416000003_create_conversations_and_messages.rb, app/models/bid.rb</files>
  <read_first>
    - app/models/bid.rb
  </read_first>
  <action>
    - Create migration for `conversations` (bid_id, task_id) and `messages` (conversation_id, sender_id, content).
    - Implement `Conversation` model with `belongs_to :bid` and `belongs_to :task`.
    - Implement `Message` model with `belongs_to :conversation` and `belongs_to :sender (User)`.
    - Add `after_create :create_conversation` callback to `Bid`.
    - Per D-12, D-14.
  </action>
  <verify>
    <automated>ls db/migrate/*_create_conversations_and_messages.rb</automated>
  </verify>
  <done>Messaging foundation is defined.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Run migrations and implement Content Filtering</name>
  <files>db/schema.rb, app/services/content_filter_service.rb, app/models/message.rb, spec/models/message_spec.rb, spec/services/content_filter_service_spec.rb</files>
  <read_first>
    - app/models/message.rb
    - spec/services/content_filter_service_spec.rb
  </read_first>
  <action>
    - Run bin/rails db:migrate.
    - Create `ContentFilterService` with `NEPAL_PHONE_REGEX` and `EMAIL_REGEX`.
    - Implement `mask(text)` method.
    - In `Message` model, add `filtered_content` method with masking logic (D-16).
    - Update `spec/services/content_filter_service_spec.rb` with various Nepal number formats.
    - Update `spec/models/message_spec.rb` to verify masking logic in the model.
    - Lift masking for Poster and Assigned Tasker once assigned.
  </action>
  <verify>
    <automated>bin/rails db:migrate && bundle exec rspec spec/services/content_filter_service_spec.rb spec/models/message_spec.rb</automated>
  </verify>
  <done>Privacy-first messaging is verified.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Conversation Access Logic</name>
  <files>app/policies/conversation_policy.rb, app/models/conversation.rb, spec/policies/conversation_policy_spec.rb</files>
  <read_first>
    - app/models/conversation.rb
    - spec/policies/conversation_policy_spec.rb
  </read_first>
  <action>
    - Create Pundit `ConversationPolicy`.
    - Allow access to Poster and the specific Bidder.
    - Implement `archive!` logic to hide threads for non-assigned bidders after assignment (D-15).
    - Update `spec/policies/conversation_policy_spec.rb` to test authorization rules.
  </action>
  <verify>
    <automated>bundle exec rspec spec/policies/conversation_policy_spec.rb</automated>
  </verify>
  <done>Messaging access control is enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Input → Message Storage | Raw PII should be filtered before display to prevent bypass. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | Message#filtered_content | mitigate | Regex-based masking with Nepal-specific patterns; masking lifted ONLY after status transitions to 'assigned'. |
</threat_model>

<verification>
Run `bundle exec rspec spec/models/message_spec.rb spec/services/content_filter_service_spec.rb`.
</verification>

<success_criteria>
- Bid creation triggers Conversation creation.
- Message content is masked for unassigned tasks.
- Masking accounts for common Nepal number variations.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trust-safety-support/05-03-SUMMARY.md`
</output>
