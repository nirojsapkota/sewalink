---
phase: "05"
plan: "04"
type: execute
wave: 3
depends_on: ["05-03"]
files_modified: [
  "app/controllers/conversations_controller.rb",
  "app/controllers/messages_controller.rb",
  "app/views/conversations/show.html.erb",
  "app/views/messages/_message.html.erb",
  "config/routes.rb",
  "spec/requests/conversations_spec.rb",
  "spec/system/messages_spec.rb"
]
autonomous: false
requirements: [SAFE-05]
user_setup: []

must_haves:
  truths:
    - "Poster can view and reply to chats for each bidder on their task"
    - "Tasker can view and reply to the chat linked to their bid"
    - "New messages appear instantly without page refresh (Turbo Streams)"
  artifacts:
    - path: "app/controllers/conversations_controller.rb"
      provides: "Secure access to chat threads"
    - path: "spec/requests/conversations_spec.rb"
      provides: "Verification of chat access and authorization"
    - path: "spec/system/messages_spec.rb"
      provides: "End-to-end testing of real-time messaging"
---

<objective>
Implement the real-time messaging UI to enable seamless coordination between Posters and Taskers.

Purpose: Facilitates negotiation and logistics within the platform.
Output: Chat UI with real-time updates via Turbo Streams.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trust-safety-support/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Conversation Controller and Basic UI</name>
  <files>app/controllers/conversations_controller.rb, config/routes.rb, app/views/conversations/show.html.erb, spec/requests/conversations_spec.rb</files>
  <read_first>
    - app/controllers/application_controller.rb
    - spec/requests/conversations_spec.rb
  </read_first>
  <action>
    - Add routes for `conversations` (show) and `messages` (create).
    - Implement `ConversationsController#show` with Pundit authorization.
    - Create a mobile-friendly chat view.
    - Update `spec/requests/conversations_spec.rb` to test access control.
  </action>
  <verify>
    <automated>bundle exec rspec spec/requests/conversations_spec.rb</automated>
  </verify>
  <done>Chat UI foundation is built.</done>
</task>

<task type="auto">
  <name>Task 2: Enable Real-time Messaging with Turbo Streams</name>
  <files>app/models/message.rb, app/views/messages/_message.html.erb, app/controllers/messages_controller.rb, spec/system/messages_spec.rb</files>
  <read_first>
    - app/models/message.rb
    - spec/system/messages_spec.rb
  </read_first>
  <action>
    - Add `broadcasts_to :conversation` to `Message` model.
    - Update `app/views/conversations/show.html.erb` to include `turbo_stream_from @conversation`.
    - Implement `MessagesController#create` that saves the message and returns a Turbo Stream response.
    - Update `spec/system/messages_spec.rb` to test real-time message delivery.
  </action>
  <verify>
    <automated>bundle exec rspec spec/system/messages_spec.rb</automated>
  </verify>
  <done>Messaging is now real-time.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Real-time Chat</name>
  <files>app/views/conversations/show.html.erb</files>
  <action>
    Manual verification of real-time chat sync.
    1. Log in as a Poster and open a task with bids.
    2. Open a chat with a Bidder in one window.
    3. Log in as that Bidder in a private/incognito window.
    4. Send messages back and forth.
    5. Verify messages appear instantly without refresh.
    6. Verify phone numbers/emails are masked if task is unassigned.
  </action>
  <verify>
    <automated>echo "Manual verification required"</automated>
  </verify>
  <done>Real-time chat is verified by human.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| ActionCable (WebSocket) → Client | Message broadcasts cross the network to all subscribers. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | ActionCable Subscription | mitigate | Ensure `turbo_stream_from` uses a signed stream name and the connection is authorized via `Connection#find_verified_user`. |
</threat_model>

<verification>
Run `bundle exec rspec spec/system/messages_spec.rb`.
</verification>

<success_criteria>
- Seamless real-time chat between parties.
- Strict authorization for conversation access.
- Content filtering applied in real-time.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trust-safety-support/05-04-SUMMARY.md`
</output>
