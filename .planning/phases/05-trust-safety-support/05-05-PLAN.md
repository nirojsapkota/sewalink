---
phase: "05"
plan: "05"
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified: [
  "app/views/profiles/_profile_info.html.erb",
  "app/views/tasks/show.html.erb",
  "app/models/dispute_evidence.rb",
  "app/controllers/dispute_evidences_controller.rb",
  "db/migrate/20260416000004_create_dispute_evidences.rb",
  "spec/system/contact_masking_spec.rb",
  "spec/requests/dispute_evidences_spec.rb"
]
autonomous: true
requirements: [SAFE-06, SAFE-04]
user_setup: []

must_haves:
  truths:
    - "User phone numbers and emails are hidden in UI until task is assigned"
    - "Poster can upload evidence (photos/videos) for disputes after task completion"
  artifacts:
    - path: "app/views/profiles/_profile_info.html.erb"
      provides: "Conditional visibility for contact info"
    - path: "app/models/dispute_evidence.rb"
      provides: "Evidence storage for resolution"
    - path: "spec/system/contact_masking_spec.rb"
      provides: "Verification of UI-level contact info protection"
    - path: "spec/requests/dispute_evidences_spec.rb"
      provides: "Verification of dispute evidence submission"
---

<objective>
Implement final privacy controls and dispute resolution evidence support to complete the trust and safety layer.

Purpose: Protects personal data from scraping/unauthorized access and provides a safety net for post-job issues.
Output: UI masking logic and dispute evidence submission flow.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trust-safety-support/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Contact Masking in UI</name>
  <files>app/views/profiles/_profile_info.html.erb, app/views/tasks/show.html.erb, app/helpers/user_helper.rb, spec/system/contact_masking_spec.rb</files>
  <read_first>
    - app/views/profiles/show.html.erb
    - spec/system/contact_masking_spec.rb
  </read_first>
  <action>
    - Update profile and task views to hide phone number and email by default.
    - Implement logic: Show contact info ONLY if `task.assigned?` and current user is the Poster or Assigned Tasker.
    - Add a helper method `mask_contact_info(text)` that returns "[HIDDEN UNTIL ASSIGNED]".
    - Update `spec/system/contact_masking_spec.rb` to test UI visibility rules.
    - Per D-13.
  </action>
  <verify>
    <automated>bundle exec rspec spec/system/contact_masking_spec.rb</automated>
  </verify>
  <done>User privacy is protected in the UI.</done>
</task>

<task type="auto">
  <name>Task 2: Define Dispute Evidence model and migrations</name>
  <files>app/models/dispute_evidence.rb, db/migrate/20260416000004_create_dispute_evidences.rb</files>
  <read_first>
    - app/models/task.rb
  </read_first>
  <action>
    - Create `DisputeEvidence` model with `belongs_to :task` and `has_many_attached :files`.
    - Create migration for `dispute_evidences` table (task_id, user_id, description).
    - Per D-10.
  </action>
  <verify>
    <automated>ls db/migrate/*_create_dispute_evidences.rb</automated>
  </verify>
  <done>Dispute evidence infrastructure is defined.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Run migrations and implement Evidence Upload UI</name>
  <files>db/schema.rb, app/controllers/dispute_evidences_controller.rb, app/views/tasks/show.html.erb, spec/requests/dispute_evidences_spec.rb</files>
  <read_first>
    - app/models/dispute_evidence.rb
    - spec/requests/dispute_evidences_spec.rb
  </read_first>
  <action>
    - Run bin/rails db:migrate.
    - Implement `DisputeEvidencesController#create` with Pundit authorization.
    - Add "Report Issue / Upload Evidence" form to Task show page for completed tasks.
    - Update `spec/requests/dispute_evidences_spec.rb` to test submission and authorization.
  </action>
  <verify>
    <automated>bin/rails db:migrate && bundle exec rspec spec/requests/dispute_evidences_spec.rb</automated>
  </verify>
  <done>Dispute resolution flow is active and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Public Profiles → Contact Info | Contact info is untrusted for public consumption until assignment. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Profiles UI | mitigate | View-level logic to hide contact info; ensured by Pundit/Controller scoping in related API calls. |
</threat_model>

<verification>
Run `bundle exec rspec spec/system/contact_masking_spec.rb spec/requests/dispute_evidences_spec.rb`.
</verification>

<success_criteria>
- Contact info visibility follows assignment status.
- Dispute evidence can be submitted post-completion.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trust-safety-support/05-05-SUMMARY.md`
</output>
