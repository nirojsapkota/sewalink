---
phase: 06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/models/task.rb, app/controllers/tasks_controller.rb, app/controllers/bids_controller.rb, app/policies/task_policy.rb, app/views/tasks/show.html.erb]
autonomous: true
requirements: [TASK-07]
must_haves:
  truths:
    - "Poster can toggle tasks between draft and open statuses"
    - "Task moves to in_progress immediately when a bidder is accepted"
    - "Tasker can 'Request Payment', moving status to pending_payment"
    - "Poster can 'Release Payment', moving status to payment_completed"
  artifacts:
    - path: "app/models/task.rb"
      provides: "Extended status enum and lifecycle hooks"
    - path: "app/controllers/tasks_controller.rb"
      provides: "Actions for manual status transitions"
  key_links:
    - from: "app/controllers/bids_controller.rb"
      to: "Task#in_progress!"
      via: "accept action"
---

<objective>
Implement the extended task lifecycle by adding new statuses and controller actions for transitions.

Purpose: To provide a granular workflow for task completion and payment release.
Output: Updated Task model, transition actions in TasksController, and role-based action buttons in the Task view.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md
@app/models/task.rb
@app/controllers/tasks_controller.rb
@app/controllers/bids_controller.rb
</context>

<tasks>

<task type="auto">
  <name>Update Task Model Enum and Lifecycle Hooks</name>
  <read_first>
    - app/models/task.rb
    - .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md (D-01, D-03)
  </read_first>
  <files>app/models/task.rb</files>
  <action>
    - Update the `status` enum to include new states: `pending_payment`, `payment_completed`, and `dispute`.
    - Final enum: `{ draft: 0, open: 1, assigned: 2, in_progress: 3, completed: 4, cancelled: 5, pending_payment: 6, payment_completed: 7, dispute: 8 }`.
    - Modify `release_escrow_if_completed` (private method) to trigger when `status` changes to `payment_completed` instead of `completed`.
  </action>
  <acceptance_criteria>
    - Task.statuses.keys includes 'pending_payment', 'payment_completed', and 'dispute'.
    - `task.payment_completed!` triggers the escrow release logic in a Rails console test.
  </acceptance_criteria>
  <verify>
    <automated>rails runner "exit (Task.statuses.has_key?('pending_payment') && Task.statuses.has_key?('payment_completed') && Task.statuses.has_key?('dispute') ? 0 : 1)"</automated>
  </verify>
  <done>Task model reflects the extended lifecycle.</done>
</task>

<task type="auto">
  <name>Implement Transition Actions and Policy</name>
  <read_first>
    - app/controllers/tasks_controller.rb
    - app/policies/task_policy.rb
    - .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md (D-02, D-04, D-06, D-07, D-08)
  </read_first>
  <files>app/controllers/tasks_controller.rb, app/policies/task_policy.rb, config/routes.rb</files>
  <action>
    - Add member routes to `tasks`: `toggle_status`, `request_payment`, `release_payment`, `mark_as_done`, `raise_dispute`.
    - Implement these actions in `TasksController`:
      - `toggle_status`: Toggle between `draft` and `open`.
      - `request_payment`: Set status to `pending_payment` (Tasker action).
      - `release_payment`: Set status to `payment_completed` (Poster action).
      - `mark_as_done`: Set status to `completed` (Poster action).
      - `raise_dispute`: Set status to `dispute`.
    - Update `TaskPolicy` (assuming Pundit is used):
      - `toggle_status?`: `user == record.user && (record.draft? || record.open?)`
      - `request_payment?`: `user == record.tasker && record.in_progress?`
      - `release_payment?`: `user == record.user && (record.pending_payment? || record.completed?)`
      - `mark_as_done?`: `user == record.user && record.in_progress?`
      - `raise_dispute?`: `(user == record.user || user == record.tasker) && (record.in_progress? || record.pending_payment? || record.completed?)`
  </action>
  <acceptance_criteria>
    - Actions correctly update the task status in the database.
    - Only authorized users can trigger specific transitions (verified via Policy).
  </acceptance_criteria>
  <verify>
    <automated>bundle exec rspec spec/requests/tasks_spec.rb # Assuming specs exist or create a minimal smoke test</automated>
  </verify>
  <done>Task transitions are functionally implemented and secured.</done>
</task>

<task type="auto">
  <name>Wire Transitions to Bids and UI</name>
  <read_first>
    - app/controllers/bids_controller.rb
    - app/views/tasks/show.html.erb
    - .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md (D-03, D-14)
  </read_first>
  <files>app/controllers/bids_controller.rb, app/views/tasks/show.html.erb</files>
  <action>
    - Update `BidsController#accept`: Change `@task.update!(status: :assigned, ...)` to `@task.update!(status: :in_progress, ...)` per D-03.
    - In `app/views/tasks/show.html.erb`, add the following action buttons (wrapped in policy checks):
      - Poster: "Open Task" / "Move to Draft" (toggle).
      - Poster: "Mark as Done" (visible when `in_progress`).
      - Tasker: "Request Payment" (visible when `in_progress`).
      - Poster: "Release Payment" (visible when `pending_payment` or `completed`).
      - Both: "I have an issue" (Dispute) (visible when `in_progress`, `pending_payment`, or `completed`).
    - Use `button_to` with `method: :patch` for these actions.
  </action>
  <acceptance_criteria>
    - Accepting a bid immediately sets the task to `in_progress`.
    - The correct buttons appear for Posters and Taskers based on the current task status.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "in_progress" app/controllers/bids_controller.rb</automated>
  </verify>
  <done>User interface allows triggering all defined lifecycle transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Unauthorized status transitions (e.g., Tasker releasing their own payment) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | TasksController | mitigate | Use Pundit (TaskPolicy) to enforce that only authorized users can trigger specific status transitions. |
| T-06-02 | Information Disclosure | Task View | mitigate | Ensure buttons/actions are only rendered if the user has the corresponding permission. |
</threat_model>

<verification>
1. Log in as Poster, create task.
2. Verify 'Move to Draft' / 'Open' toggle works.
3. Accept a bid, verify status becomes 'In Progress'.
4. Log in as Tasker, verify 'Request Payment' moves status to 'Pending Payment'.
5. Log in as Poster, verify 'Release Payment' moves status to 'Payment Completed'.
</verification>

<success_criteria>
- Tasks can navigate the full lifecycle from draft to payment_completed.
- All transitions are protected by role-based authorization.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-01-SUMMARY.md`
</output>
