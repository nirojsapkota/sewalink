---
phase: 06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "app/models/task.rb",
  "app/controllers/tasks_controller.rb",
  "config/routes.rb",
  "db/migrate/*.rb",
  "spec/models/task_spec.rb",
  "spec/requests/tasks_spec.rb"
]
autonomous: true
requirements: [TASK-07]
user_setup: []

must_haves:
  truths:
    - "Task can be in draft, open, in_progress, pending_payment, payment_completed, completed, or dispute status"
    - "Poster can toggle between draft and open status"
    - "Tasker can request payment, moving task to pending_payment"
    - "Poster can release payment, moving task to payment_completed then completed"
    - "Either party can raise a dispute"
  artifacts:
    - path: "app/models/task.rb"
      provides: "Extended status enum and transition logic"
    - path: "app/controllers/tasks_controller.rb"
      provides: "Actions for request_payment, release_payment, raise_dispute, toggle_draft"
  key_links:
    - from: "app/models/task.rb"
      to: "status enum"
      via: "ActiveRecord enum declaration"
    - from: "app/controllers/tasks_controller.rb"
      to: "Pundit policies"
      via: "authorize @task"
---

<objective>
Implement the extended task lifecycle by adding new statuses and controller actions for transition management.

Purpose: Provide a granular and secure workflow for tasks from draft to final completion and payment release.
Output: Updated Task model status enum, new controller actions for transitions, and supporting tests.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-RESEARCH.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md

<interfaces>
From app/models/task.rb (current state):
```ruby
class Task < ApplicationRecord
  enum :status, { draft: 0, open: 1, in_progress: 2, completed: 3, cancelled: 4 }
end
```

From app/models/payment_transaction.rb (state machine reference):
```ruby
class PaymentTransaction < ApplicationRecord
  include AASM
  aasm column: :state do
    state :pending, initial: true
    state :completed, :failed
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Task status enum and model logic</name>
  <files>app/models/task.rb, db/migrate/20260415_add_new_statuses_to_tasks.rb, spec/models/task_spec.rb</files>
  <behavior>
    - Task status enum supports: draft, open, in_progress, pending_payment, payment_completed, completed, dispute, cancelled.
    - Method `request_payment!` transitions status to `pending_payment` if currently `in_progress`.
    - Method `release_payment!` transitions status to `payment_completed` then `completed` if currently `pending_payment`.
    - Method `raise_dispute!` transitions status to `dispute`.
    - Method `toggle_draft!` toggles between `draft` and `open`.
  </behavior>
  <action>
    - Create a migration to ensure the `status` column in `tasks` can handle the new enum values (if it's an integer, update comment or range; if string, ensure no issues). Per D-01, D-02, D-03, D-05, D-09.
    - Update the `status` enum in `app/models/task.rb`.
    - Add logic for status transitions. Consider using `AASM` if logic becomes complex (per RESEARCH.md pattern), otherwise use custom methods with state-locked guards.
    - Implement transition controls: only Poster can `release_payment!`, only Tasker can `request_payment!`.
  </action>
  <verify>
    <automated>bundle exec rspec spec/models/task_spec.rb</automated>
  </verify>
  <done>Task model correctly handles all new statuses and transition guards pass tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Controller Actions for Transitions</name>
  <files>app/controllers/tasks_controller.rb, config/routes.rb, spec/requests/tasks_spec.rb</files>
  <behavior>
    - POST /tasks/:id/request_payment: Updates status to pending_payment, returns 200 or redirect.
    - POST /tasks/:id/release_payment: Updates status to completed, returns 200 or redirect.
    - POST /tasks/:id/raise_dispute: Updates status to dispute, returns 200 or redirect.
    - PATCH /tasks/:id/toggle_draft: Toggles status between draft/open.
    - Actions are protected by Pundit policies.
  </behavior>
  <action>
    - Add member routes to `resources :tasks` in `config/routes.rb`.
    - Implement `request_payment`, `release_payment`, `raise_dispute`, and `toggle_draft` in `TasksController`.
    - Per D-04, D-06, D-07, D-08, D-14.
    - Ensure `release_payment` also triggers existing `LedgerManager` to release escrow (Integration with Phase 03 logic).
    - Add user confirmation requirements in the UI (to be fully implemented in 06-03, but ensure the controller accepts a confirm parameter or just performs the action).
  </action>
  <verify>
    <automated>bundle exec rspec spec/requests/tasks_spec.rb</automated>
  </verify>
  <done>Controller actions are implemented and authorized, with request specs passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Poster/Tasker → TasksController | Users attempt to trigger status transitions they shouldn't (e.g., Tasker releasing their own payment) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Elevation of Privilege | TasksController#release_payment | mitigate | Strict Pundit check: only poster of the task can release payment. |
| T-06-01-02 | Elevation of Privilege | TasksController#request_payment | mitigate | Strict Pundit check: only assigned tasker can request payment. |
| T-06-01-03 | Denial of Service | Task status spamming | mitigate | Rate limit status changes or require meaningful state progression. |
</threat_model>

<verification>
1. Run `bundle exec rspec spec/models/task_spec.rb` to verify model transitions.
2. Run `bundle exec rspec spec/requests/tasks_spec.rb` to verify controller authorization and success.
</verification>

<success_criteria>
- All 7 statuses are available and functional in the Task model.
- Unauthorized users cannot trigger status transitions (Pundit working).
- Transitions between draft/open, in_progress -> pending_payment, pending_payment -> completed are verified.
</success_criteria>

<output>
After completion, create .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-01-SUMMARY.md
</output>
