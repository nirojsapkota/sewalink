---
phase: 06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "spec/rails_helper.rb",
  "app/models/task.rb",
  "db/migrate/*.rb",
  "spec/models/task_spec.rb"
]
autonomous: true
requirements: [TASK-07]
user_setup: []

must_haves:
  truths:
    - "Task model supports the full lifecycle enum (draft, open, in_progress, pending_payment, payment_completed, completed, dispute, cancelled)"
    - "Model-level guards prevent invalid state transitions"
    - "RSpec is configured for ActionCable and System testing"
  artifacts:
    - path: "app/models/task.rb"
      provides: "Updated enum and transition methods"
    - path: "spec/rails_helper.rb"
      provides: "Testing support for ActionCable"
  key_links:
    - from: "app/models/task.rb"
      to: "enum :status"
      via: "ActiveRecord enum declaration"
---

<objective>
Setup testing infrastructure for real-time updates and implement the extended task status lifecycle in the model.

Purpose: Provide a solid, tested foundation for the task workflow before adding UI and real-time layers.
Output: Updated Task model status enum, transition methods, and ActionCable testing support.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-RESEARCH.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md

<interfaces>
From app/models/task.rb (current):
```ruby
class Task < ApplicationRecord
  enum :status, { draft: 0, open: 1, in_progress: 2, completed: 3, cancelled: 4 }
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Setup ActionCable testing infrastructure</name>
  <files>spec/rails_helper.rb</files>
  <action>
    - Update `spec/rails_helper.rb` to include `ActionCable::TestHelper`.
    - Ensure `capybara` is configured correctly for system tests (typically uses `:selenium_chrome_headless`).
  </action>
  <verify>
    <automated>grep "ActionCable::TestHelper" spec/rails_helper.rb</automated>
  </verify>
  <done>RSpec is configured to test ActionCable broadcasts and system interactions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Update Task status enum and transitions</name>
  <files>app/models/task.rb, db/migrate/20260415_add_new_statuses_to_tasks.rb, spec/models/task_spec.rb</files>
  <behavior>
    - Task status enum: { draft: 0, open: 1, in_progress: 2, pending_payment: 3, payment_completed: 4, completed: 5, dispute: 6, cancelled: 7 } (or similar mapping).
    - `request_payment!`: in_progress -> pending_payment.
    - `release_payment!`: pending_payment -> payment_completed -> completed (automatic).
    - `raise_dispute!`: any active state -> dispute.
    - `toggle_draft!`: draft <-> open.
  </behavior>
  <action>
    - Create and run a migration to update the `status` column enum comments/range.
    - Update the `status` enum in `app/models/task.rb` per D-01, D-02, D-03, D-05.
    - Implement model-level transition methods with guards. Use AASM if preferred (already used in PaymentTransaction) or direct ActiveRecord methods.
  </action>
  <verify>
    <automated>bundle exec rspec spec/models/task_spec.rb</automated>
  </verify>
  <done>Task model correctly implements the extended lifecycle with all 8 statuses and transition guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model Transitions | Internal logic must ensure states cannot be bypassed (e.g., draft -> completed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | Task#status | mitigate | Use strictly guarded methods for status transitions instead of direct attribute updates. |
</threat_model>

<verification>
Run `bundle exec rspec spec/models/task_spec.rb` to ensure all status transitions and guards work as expected.
</verification>

<success_criteria>
- Task model has 8 functional statuses.
- Model tests pass for all valid and invalid transitions.
- Testing infrastructure is ready for subsequent waves.
</success_criteria>

<output>
After completion, create .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-01-SUMMARY.md
</output>
