---
phase: 06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [
  "app/models/task.rb",
  "app/models/bid.rb",
  "app/javascript/controllers/toast_controller.js",
  "app/views/notifications/_toast.html.erb",
  "app/views/tasks/_task.html.erb",
  "app/views/layouts/application.html.erb"
]
autonomous: true
requirements: [TASK-08]
user_setup: []

must_haves:
  truths:
    - "Poster receives a real-time notification (toast) when a new bid is placed"
    - "Task status badges update in real-time when changed by either party"
    - "Notifications are targeted only to relevant users"
  artifacts:
    - path: "app/javascript/controllers/toast_controller.js"
      provides: "Client-side toast display logic"
    - path: "app/views/notifications/_toast.html.erb"
      provides: "HTML structure for notifications"
  key_links:
    - from: "app/models/task.rb"
      to: "Turbo Stream"
      via: "broadcast_to"
    - from: "app/views/layouts/application.html.erb"
      to: "Turbo Stream channel"
      via: "turbo_stream_from"
---

<objective>
Implement real-time UI updates and background notifications using Turbo Streams.

Purpose: Improve marketplace responsiveness by showing instant feedback to users without manual page refreshes.
Output: Real-time status badge updates and a toast notification system for background events.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-RESEARCH.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Targeted Turbo Stream Broadcasts</name>
  <files>app/models/task.rb, app/models/bid.rb</files>
  <action>
    - In `Bid`, add `after_create_commit` to broadcast a toast notification to the task's poster. Per D-10, D-11.
    - In `Task`, add `after_update_commit` to broadcast status changes to both the poster and the assigned tasker (if present).
    - Use `broadcast_prepend_to` for notifications and `broadcast_replace_to` for inline task updates.
    - Ensure streams are scoped (e.g., `broadcast_prepend_to [poster, :notifications]`).
  </action>
  <verify>
    <automated>MISSING — ActionCable testing is complex; manual verification required in next task</automated>
  </verify>
  <done>Models are configured to broadcast targeted updates upon relevant state changes.</done>
</task>

<task type="auto">
  <name>Task 2: Build Toast Notification System & UI Sync</name>
  <files>app/javascript/controllers/toast_controller.js, app/views/notifications/_toast.html.erb, app/views/layouts/application.html.erb, app/views/tasks/_task.html.erb</files>
  <action>
    - Create `app/javascript/controllers/toast_controller.js` to handle appearing/disappearing animations (using Tailwind classes if available).
    - Create `app/views/notifications/_toast.html.erb` partial.
    - Add `<div id="notifications" data-controller="toast"></div>` to `application.html.erb`.
    - Add `turbo_stream_from [current_user, :notifications]` to relevant layouts or views.
    - Update `app/views/tasks/_task.html.erb` to ensure the status badge and action buttons are wrapped in identifiable DOM IDs for Turbo Stream replacement.
    - Per D-12.
  </action>
  <verify>
    <automated>MISSING — Wave 2 requires manual verification</automated>
  </verify>
  <done>Toast notifications appear for background events and task UI updates instantly when status changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Real-time notification and UI update system.</what-built>
  <how-to-verify>
    1. Open the app in two different browsers (or one normal, one incognito).
    2. Log in as a Poster in Window A and a Tasker in Window B.
    3. In Window B, place a bid on one of the Poster's tasks.
    4. Verify a toast appears in Window A: "New bid received".
    5. In Window A, assign the task to the Tasker.
    6. Verify Window B's task view updates the status to "In Progress" without a refresh.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ActionCable → Client | Sensitive data might be broadcast to the wrong stream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | Turbo Stream Broadcasts | mitigate | Use strictly scoped stream names like `[user, :notifications]` to prevent broadcasting private data to unauthorized users. |
| T-06-02-02 | Denial of Service | Broadcast storm | mitigate | Limit the frequency of broadcasts per model update if necessary. |
</threat_model>

<verification>
1. Manual verification as described in the checkpoint.
2. Ensure no JS errors in the console during Turbo Stream events.
</verification>

<success_criteria>
- Toast notifications appear on the screen when a new bid is received by the poster.
- Task status badges update automatically on the show and index pages when changed.
- No full page refresh is required for the user to see these updates.
</success_criteria>

<output>
After completion, create .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-02-SUMMARY.md
</output>
