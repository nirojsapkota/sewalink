---
phase: 06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [
  "app/controllers/tasks_controller.rb",
  "app/policies/task_policy.rb",
  "config/routes.rb",
  "app/views/tasks/_task_actions.html.erb",
  "spec/requests/tasks_status_spec.rb",
  "spec/system/task_status_system_spec.rb"
]
autonomous: true
requirements: [TASK-07]
user_setup: []

must_haves:
  truths:
    - "Tasker can request payment via a POST request when task is in_progress"
    - "Poster can release payment via a POST request when task is pending_payment"
    - "Unauthorized users cannot trigger transitions (Pundit verified)"
    - "Poster can toggle draft/open via UI"
  artifacts:
    - path: "app/controllers/tasks_controller.rb"
      provides: "Status transition actions"
    - path: "app/views/tasks/_task_actions.html.erb"
      provides: "Buttons for transitions"
  key_links:
    - from: "app/controllers/tasks_controller.rb"
      to: "Pundit"
      via: "authorize @task, :request_payment?"
---

<objective>
Implement controller actions and UI buttons for task status transitions with full authorization checks.

Purpose: Provide user-facing mechanisms to progress tasks through the lifecycle securely.
Output: New controller actions, Pundit policies, and UI transition buttons.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-RESEARCH.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Controller actions and authorization</name>
  <files>app/controllers/tasks_controller.rb, config/routes.rb, app/policies/task_policy.rb, spec/requests/tasks_status_spec.rb</files>
  <behavior>
    - POST /tasks/:id/request_payment: task status -> pending_payment (only for assigned tasker).
    - POST /tasks/:id/release_payment: task status -> completed (only for poster).
    - POST /tasks/:id/raise_dispute: task status -> dispute (only for poster or tasker).
    - PATCH /tasks/:id/toggle_draft: task status toggle draft/open (only for poster).
  </behavior>
  <action>
    - Define member routes for transitions in `config/routes.rb`.
    - Implement actions in `TasksController` using transition methods from `Task` model.
    - Update `TaskPolicy` with specific authorization rules for each action.
    - Integrate `release_payment` with Phase 03 `LedgerManager.release_escrow` logic per D-07, D-09.
  </action>
  <verify>
    <automated>bundle exec rspec spec/requests/tasks_status_spec.rb</automated>
  </verify>
  <done>Controller actions are implemented, authorized, and passing request specs.</done>
</task>

<task type="auto">
  <name>Task 2: Add status transition buttons to UI</name>
  <files>app/views/tasks/_task_actions.html.erb, app/views/tasks/show.html.erb, spec/system/task_status_system_spec.rb</files>
  <action>
    - Create or update `app/views/tasks/_task_actions.html.erb`.
    - Show buttons conditionally:
        - "Request Payment" (Tasker view) if status == `in_progress`.
        - "Release Payment" and "I have an issue" (Poster view) if status == `pending_payment`.
        - "Toggle Draft/Open" (Poster view) if status in `[draft, open]`.
    - Ensure buttons use `button_to` with appropriate HTTP verbs.
    - Per D-04, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>bundle exec rspec spec/system/task_status_system_spec.rb</automated>
  </verify>
  <done>Transition buttons appear correctly in the UI and function as expected in system tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → TasksController | Users must not be able to trigger transitions on tasks they don't own/participate in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Elevation of Privilege | request_payment action | mitigate | Verify `current_user == task.assigned_tasker` in Pundit policy. |
| T-06-02-02 | Elevation of Privilege | release_payment action | mitigate | Verify `current_user == task.user` in Pundit policy. |
</threat_model>

<verification>
Run `bundle exec rspec spec/requests/tasks_status_spec.rb` and `bundle exec rspec spec/system/task_status_system_spec.rb`.
</verification>

<success_criteria>
- Taskers can request payment.
- Posters can release payment.
- Unauthorized users receive 403 Forbidden.
- UI buttons are correctly displayed for each role and state.
</success_criteria>

<output>
After completion, create .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-02-SUMMARY.md
</output>
