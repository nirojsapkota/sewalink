---
phase: 06
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [app/models/task.rb, app/models/bid.rb, app/javascript/controllers/toast_controller.js, app/views/layouts/application.html.erb, app/views/shared/_toast.html.erb]
autonomous: true
requirements: [TASK-08]
must_haves:
  truths:
    - "New bids appear in real-time on the Task show page"
    - "Status changes trigger a Toast notification for relevant users"
    - "Users see real-time updates without refreshing the page"
  artifacts:
    - path: "app/javascript/controllers/toast_controller.js"
      provides: "Auto-hiding notification logic"
    - path: "app/views/shared/_toast.html.erb"
      provides: "Partial for dynamic notification delivery"
---

<objective>
Implement real-time UI updates and background notifications using Hotwire Turbo Streams and Stimulus.

Purpose: To provide immediate feedback to users when status changes or new bids arrive.
Output: Targeted broadcasts in models, a Stimulus-powered Toast system, and real-time updates on the task view.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md
@app/models/task.rb
@app/models/bid.rb
</context>

<tasks>

<task type="auto">
  <name>Implement Targeted Turbo Stream Broadcasts</name>
  <read_first>
    - app/models/task.rb
    - app/models/bid.rb
    - .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md (D-10, D-11)
  </read_first>
  <files>app/models/task.rb, app/models/bid.rb</files>
  <action>
    - Update `Task` model:
      - Replace `broadcasts_refreshes` with explicit broadcasts if needed for targeting, or keep it and add stream broadcasts.
      - Add `after_update_commit -> { broadcast_replace_to self }` for status updates.
      - Add targeted status change notification: `after_update_commit -> { broadcast_prepend_to "notifications_#{user_id}", target: "toasts", partial: "shared/toast", locals: { message: "Task status updated: #{status}" } }`.
      - Add targeted status change for tasker: `after_update_commit -> { broadcast_prepend_to "notifications_#{tasker_id}", target: "toasts", partial: "shared/toast", locals: { message: "Task status updated: #{status}" } } if tasker_id`.
    - Update `Bid` model:
      - Add `after_create_commit -> { broadcast_prepend_to "notifications_#{task.user_id}", target: "toasts", partial: "shared/toast", locals: { message: "New bid received for: #{task.title}" } }`.
      - Ensure bids refresh on the task page: `after_create_commit -> { broadcast_append_to task, target: "bids" }`.
  </action>
  <acceptance_criteria>
    - Status changes trigger broadcasts to the relevant user's notification stream.
    - New bids trigger a broadcast to the poster's notification stream.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "broadcast_prepend_to" app/models/task.rb</automated>
  </verify>
  <done>Models are configured for targeted real-time broadcasts.</done>
</task>

<task type="auto">
  <name>Create Toast Notification System</name>
  <read_first>
    - app/views/layouts/application.html.erb
    - .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md (D-12)
  </read_first>
  <files>app/javascript/controllers/toast_controller.js, app/views/shared/_toast.html.erb, app/views/layouts/application.html.erb</files>
  <action>
    - Create `app/views/shared/_toast.html.erb`:
      - A div with `data-controller="toast"` and Tailwind styling (fixed position, top-right).
      - Include the message passed in locals.
    - Create `app/javascript/controllers/toast_controller.js`:
      - In `connect()`, set a timeout (e.g., 5 seconds) to remove the element.
      - Implement a `close()` method to remove the element immediately.
    - Update `app/views/layouts/application.html.erb`:
      - Add a container `<div id="toasts" class="fixed top-5 right-5 z-50"></div>`.
      - Add a `turbo_stream_from "notifications_#{current_user.id}" if user_signed_in?`.
  </action>
  <acceptance_criteria>
    - The `toasts` container exists in the layout.
    - The Stimulus controller successfully removes the toast after a delay.
    - User is subscribed to their own notification stream.
  </acceptance_criteria>
  <verify>
    <automated>ls app/javascript/controllers/toast_controller.js</automated>
  </verify>
  <done>Toast system is integrated into the layout and responsive to Turbo Streams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client | Sensitive data in Turbo Stream broadcasts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | Turbo Stream | mitigate | Use targeted streams (`notifications_#{user_id}`) to ensure users only receive notifications intended for them. |
</threat_model>

<verification>
1. Open two browsers, log in as Poster and Tasker.
2. In Poster browser, view a task.
3. In Tasker browser, place a bid.
4. Verify Poster browser immediately shows a Toast notification "New bid received".
5. In Poster browser, change task status.
6. Verify Tasker browser immediately shows a Toast notification "Task status updated".
</verification>

<success_criteria>
- Real-time updates for bids and status changes without page refresh.
- Persistent background notifications via Toasts for active users.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-02-SUMMARY.md`
</output>
