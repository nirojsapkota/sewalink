---
phase: 06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [
  "app/controllers/posters/dashboards_controller.rb",
  "app/views/posters/dashboards/show.html.erb",
  "app/views/posters/dashboards/_task_list.html.erb",
  "app/views/tasks/_task_actions.html.erb",
  "app/javascript/controllers/confirmation_controller.js"
]
autonomous: true
requirements: [TASK-09]
user_setup: []

must_haves:
  truths:
    - "Poster can filter their dashboard tasks by status (All, Drafts, Active, Completed, Dispute)"
    - "Filters update the task list using Turbo Frames without a full page reload"
    - "Critical status changes require user confirmation via a modal or browser dialog"
  artifacts:
    - path: "app/views/posters/dashboards/_task_list.html.erb"
      provides: "Filtered task list wrapped in a Turbo Frame"
    - path: "app/javascript/controllers/confirmation_controller.js"
      provides: "UX for confirming actions"
  key_links:
    - from: "app/views/posters/dashboards/show.html.erb"
      to: "Turbo Frame"
      via: "turbo_frame_tag 'tasks_list'"
    - from: "app/views/tasks/_task_actions.html.erb"
      to: "TasksController"
      via: "button_to request_payment_task_path"
---

<objective>
Refine the Poster dashboard with status filtering and enhance UX with confirmation dialogs.

Purpose: Provide an organized view for Posters to manage many tasks and prevent accidental status changes.
Output: A tabbed dashboard interface and confirmation UI for status transitions.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-RESEARCH.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Tabbed Dashboard Filtering</name>
  <files>app/controllers/posters/dashboards_controller.rb, app/views/posters/dashboards/show.html.erb, app/views/posters/dashboards/_task_list.html.erb</files>
  <action>
    - Update `Posters::DashboardsController#show` to handle a `status` query parameter.
    - Implement status-based filtering (All | Drafts | Active | Completed | Dispute). Map 'Active' to `open` and `in_progress`.
    - Wrap the task list in `turbo_frame_tag "tasks_list"` in both `show.html.erb` and the partial.
    - Create a horizontal tab bar with links targeting the `tasks_list` frame.
    - Per D-13.
  </action>
  <verify>
    <automated>MISSING — Manual verification in next task</automated>
  </verify>
  <done>Poster dashboard features status-based filtering that works without full page reloads.</done>
</task>

<task type="auto">
  <name>Task 2: Add Confirmation Dialogs & Refine Actions</name>
  <files>app/javascript/controllers/confirmation_controller.js, app/views/tasks/_task_actions.html.erb, app/views/tasks/_task.html.erb</files>
  <action>
    - Create `app/javascript/controllers/confirmation_controller.js` to intercept clicks and show a custom confirmation dialog (or use `data: { turbo_confirm: ... }` for native).
    - Refine `_task_actions.html.erb` to show:
      - "Request Payment" for Tasker when `in_progress`.
      - "Release Payment" and "I have an issue" (Raise Dispute) for Poster when `pending_payment`.
      - "Move to Draft" / "Make Open" for Poster when in `open` / `draft`.
    - Apply the confirmation controller to "Release Payment" and "Raise Dispute".
    - Per D-06, D-07, D-08, D-14.
  </action>
  <verify>
    <automated>MISSING — Wave 2 requires manual verification</automated>
  </verify>
  <done>Critical actions require confirmation, and the dashboard UI is polished with conditional action buttons.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Refined Poster Dashboard and Action UI.</what-built>
  <how-to-verify>
    1. Log in as a Poster.
    2. Go to My Dashboard.
    3. Click the "Drafts" tab and verify only draft tasks are shown.
    4. Click the "All" tab and verify the list updates correctly within the frame.
    5. Navigate to a task that is "Pending Payment" (request payment as a tasker first if needed).
    6. Click "Release Payment" and verify the confirmation dialog appears.
    7. Cancel the confirmation and verify no transition occurs.
    8. Confirm the release and verify the status updates to "Completed".
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side confirmation → Server | Confirmation is purely for UX; server must still validate the transition (covered in 06-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | Filtered Task List | mitigate | Ensure the controller scopes tasks to `current_user.tasks` before applying the status filter. |
</threat_model>

<verification>
1. Manual verification of the tabbed filtering.
2. Manual verification of the confirmation dialogs.
</verification>

<success_criteria>
- Poster dashboard filter tabs work correctly with Turbo Frames.
- "Release Payment" and "Raise Dispute" actions trigger a confirmation dialog.
- Action buttons are correctly shown or hidden based on the task's current status and user's role.
</success_criteria>

<output>
After completion, create .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-03-SUMMARY.md
</output>
