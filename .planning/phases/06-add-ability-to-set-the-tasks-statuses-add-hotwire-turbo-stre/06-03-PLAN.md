---
phase: 06
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [app/controllers/tasks_controller.rb, app/views/tasks/index.html.erb, app/views/tasks/_task.html.erb]
autonomous: true
requirements: [TASK-09]
must_haves:
  truths:
    - "Poster can filter their tasks using horizontal tabs (All | Drafts | Active | Completed)"
    - "Critical status changes (Release Payment, Dispute) trigger a confirmation dialog"
    - "Task status badges are visually distinct and consistent across the app"
  artifacts:
    - path: "app/views/tasks/_tabs.html.erb"
      provides: "Reusable tab-based navigation for filtering"
---

<objective>
Refine the Poster dashboard with status filtering and enhance the overall UX with confirmation dialogs and improved visual feedback.

Purpose: To help Posters manage multiple tasks efficiently and prevent accidental critical status changes.
Output: Tab-based filtering on the Poster dashboard, confirmation dialogs for sensitive actions, and consistent status UI.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md
@app/controllers/tasks_controller.rb
@app/views/tasks/index.html.erb
</context>

<tasks>

<task type="auto">
  <name>Implement Poster Dashboard Status Filtering</name>
  <read_first>
    - app/controllers/tasks_controller.rb
    - app/views/tasks/index.html.erb
    - .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md (D-13)
  </read_first>
  <files>app/controllers/tasks_controller.rb, app/views/tasks/index.html.erb, app/views/tasks/_tabs.html.erb</files>
  <action>
    - Update `TasksController#index` for posters:
      - Accept a `status` parameter (e.g., `all`, `draft`, `active`, `completed`).
      - Map `active` to statuses like `open`, `in_progress`, `pending_payment`.
      - Filter `@tasks` based on the status parameter.
    - Create `app/views/tasks/_tabs.html.erb`:
      - Implement a horizontal tab bar using Tailwind CSS.
      - Tabs: "All", "Drafts", "Active", "Completed".
      - Link each tab to `tasks_path(status: '...')` with active state styling.
    - Include `render 'tabs'` in `app/views/tasks/index.html.erb` for posters.
  </action>
  <acceptance_criteria>
    - Poster sees tabs on the index page.
    - Clicking a tab correctly filters the task list by status.
    - The active tab is visually highlighted.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "params\[:status\]" app/controllers/tasks_controller.rb</automated>
  </verify>
  <done>Poster dashboard supports status-based filtering.</done>
</task>

<task type="auto">
  <name>Add Confirmation Dialogs and UI Polishing</name>
  <read_first>
    - app/views/tasks/show.html.erb
    - app/views/tasks/_task.html.erb
    - .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md (D-14, D-12)
  </read_first>
  <files>app/views/tasks/show.html.erb, app/views/tasks/_task.html.erb</files>
  <action>
    - Add `data: { turbo_confirm: "Are you sure you want to release the payment?" }` to the "Release Payment" button.
    - Add `data: { turbo_confirm: "Are you sure you want to raise a dispute?" }` to the "I have an issue" button.
    - Update the status badge in `_task.html.erb` and `show.html.erb`:
      - Use distinct Tailwind colors for different status groups (e.g., Gray for Draft, Blue for Open, Yellow for In Progress, Green for Completed, Red for Dispute).
    - Ensure all status labels are localized using `t('.status_name')`.
  </action>
  <acceptance_criteria>
    - Confirmation dialogs appear for "Release Payment" and "Dispute" actions.
    - Status badges have color-coding corresponding to their state.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "turbo_confirm" app/views/tasks/show.html.erb</automated>
  </verify>
  <done>Critical actions are protected by confirmations and status UI is improved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → UI | Accidental triggering of irreversible financial actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Repudiation | Tasks View | mitigate | Use confirmation dialogs (`turbo_confirm`) for high-stakes actions like releasing escrowed funds or initiating disputes. |
</threat_model>

<verification>
1. Log in as Poster, navigate to Dashboard.
2. Click through tabs (Drafts, Active, Completed) and verify filtering works.
3. Open an active task.
4. Attempt to "Release Payment" and verify a browser confirmation dialog appears.
5. Verify status badges are colored appropriately on both index and show pages.
</verification>

<success_criteria>
- Seamless task management for Posters through filtering.
- Reduced risk of accidental status transitions through confirmations.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-03-SUMMARY.md`
</output>
