---
phase: 06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [
  "app/models/task.rb",
  "app/models/bid.rb",
  "app/javascript/controllers/toast_controller.js",
  "app/views/notifications/_toast.html.erb",
  "app/views/tasks/_task.html.erb",
  "app/views/layouts/application.html.erb",
  "spec/system/real_time_updates_spec.rb"
]
autonomous: true
requirements: [TASK-08]
user_setup: []

must_haves:
  truths:
    - "New bids trigger a real-time toast notification for the task poster"
    - "Task status changes automatically update the badge in the UI without refresh"
    - "Notifications are correctly scoped to the intended recipient"
  artifacts:
    - path: "app/javascript/controllers/toast_controller.js"
      provides: "Client-side toast lifecycle management"
    - path: "app/views/notifications/_toast.html.erb"
      provides: "Flash notification structure"
  key_links:
    - from: "app/models/task.rb"
      to: "broadcast_replace_to"
      via: "after_update_commit"
---

<objective>
Implement real-time UI synchronization and background notifications using Turbo Streams.

Purpose: Enhance the marketplace experience with immediate feedback and responsive status updates.
Output: Automatic UI updates for bids and task status, plus a persistent toast notification system.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-RESEARCH.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-CONTEXT.md
@.planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Turbo Stream broadcasts in models</name>
  <files>app/models/task.rb, app/models/bid.rb, spec/system/real_time_updates_spec.rb</files>
  <behavior>
    - Bid creation broadcasts a toast to `[task.user, :notifications]`.
    - Task update broadcasts a replacement to `task` for both poster and assigned tasker.
  </behavior>
  <action>
    - Add `after_create_commit` in `Bid` to broadcast notifications.
    - Add `after_update_commit` in `Task` to broadcast status changes.
    - Per D-10, D-11.
  </action>
  <verify>
    <automated>bundle exec rspec spec/system/real_time_updates_spec.rb</automated>
  </verify>
  <done>Models are configured to broadcast updates, and system tests verify the reception of stream content.</done>
</task>

<task type="auto">
  <name>Task 2: Build toast notification UI and sync logic</name>
  <files>app/javascript/controllers/toast_controller.js, app/views/notifications/_toast.html.erb, app/views/layouts/application.html.erb, app/views/tasks/_task.html.erb</files>
  <action>
    - Implement `toast_controller.js` for automatic dismissal and animations.
    - Create `_toast.html.erb` partial for the notification content.
    - Add `turbo_stream_from [current_user, :notifications]` to `application.html.erb`.
    - Ensure `_task.html.erb` has a stable DOM ID (e.g., `dom_id(task)`) for targeted replacement.
    - Per D-12.
  </action>
  <verify>
    <automated>bundle exec rspec spec/system/real_time_updates_spec.rb</automated>
  </verify>
  <done>The UI correctly displays toast notifications and updates task elements in real-time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ActionCable → Browser | Private notification content must only be broadcast to the owner's stream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | Notifications stream | mitigate | Ensure stream name is signed and includes `user.id` to prevent cross-user subscription. |
</threat_model>

<verification>
Run `bundle exec rspec spec/system/real_time_updates_spec.rb`. This test should open two sessions and verify that an action in one session reflects in the other.
</verification>

<success_criteria>
- Bid creation results in a toast appearing for the poster.
- Task status change results in the task badge updating for both parties.
- All real-time updates happen without page refresh.
</success_criteria>

<output>
After completion, create .planning/phases/06-add-ability-to-set-the-tasks-statuses-add-hotwire-turbo-stre/06-03-SUMMARY.md
</output>
