---
phase: 07-admin-panel-and-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Gemfile, Gemfile.lock, app/controllers/admin/base_controller.rb, app/controllers/admin/dashboards_controller.rb, app/controllers/admin/payouts_controller.rb, app/views/layouts/admin.html.erb, config/routes.rb, spec/requests/admin/dashboards_spec.rb, spec/factories/users.rb]
autonomous: true
requirements: [ADMIN-01]
must_haves:
  truths:
    - "Admin users can access /admin and see the dashboard"
    - "Non-admin users are redirected to root with an alert when accessing /admin"
    - "Chartkick and Groupdate gems are installed and available"
    - "Admin namespace uses the specialized admin layout"
  artifacts:
    - path: "app/controllers/admin/base_controller.rb"
      provides: "Centralized admin authorization and layout"
    - path: "app/controllers/admin/dashboards_controller.rb"
      provides: "Entry point for analytics"
    - path: "app/views/layouts/admin.html.erb"
      provides: "Specialized UI for administrative tasks"
    - path: "spec/requests/admin/dashboards_spec.rb"
      provides: "Automated verification of admin access control"
  key_links:
    - from: "app/controllers/admin/dashboards_controller.rb"
      to: "app/controllers/admin/base_controller.rb"
      via: "inheritance"
    - from: "app/controllers/admin/payouts_controller.rb"
      to: "app/controllers/admin/base_controller.rb"
      via: "inheritance"
---

<objective>
Bootstrap the admin panel infrastructure, install analytics dependencies, and set up base testing for the admin dashboard.

Purpose: Establish a secure, centralized foundation for all administrative and analytics features in Phase 7.
Output: Admin base controller, basic dashboard, admin layout, and analytics gems.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-panel-and-analytics/07-RESEARCH.md

<interfaces>
Existing Admin::PayoutsController:
```ruby
class Admin::PayoutsController < ApplicationController
  before_action :authenticate_user!
  before_action :ensure_admin!
  # ...
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Analytics Dependencies</name>
  <files>Gemfile, Gemfile.lock</files>
  <action>
    Add `chartkick` and `groupdate` gems to the Gemfile.
    Run `bundle install`.
  </action>
  <verify>
    <automated>bundle info chartkick && bundle info groupdate</automated>
  </verify>
  <done>Gems installed and registered in Gemfile.lock</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bootstrap Admin Controllers and Layout</name>
  <files>app/controllers/admin/base_controller.rb, app/controllers/admin/dashboards_controller.rb, app/controllers/admin/payouts_controller.rb, app/views/layouts/admin.html.erb, config/routes.rb</files>
  <behavior>
    - Admin::BaseController ensures user is authenticated and is an admin.
    - Admin::BaseController uses 'admin' layout.
    - Admin::DashboardsController inherits from Admin::BaseController.
    - Admin::PayoutsController inherits from Admin::BaseController and removes redundant checks.
    - GET /admin routes to Admin::DashboardsController#show.
  </behavior>
  <action>
    1. Create `app/controllers/admin/base_controller.rb`:
       - Inherit from `ApplicationController`.
       - Add `layout 'admin'`.
       - Add `before_action :authenticate_user!`.
       - Add `before_action :ensure_admin!`.
       - Implement `ensure_admin!` (redirect to root with alert if not admin).
    2. Create `app/controllers/admin/dashboards_controller.rb`:
       - Inherit from `Admin::BaseController`.
       - Define `show` action (empty for now).
    3. Modify `app/controllers/admin/payouts_controller.rb`:
       - Change parent class to `Admin::BaseController`.
       - Remove `before_action :authenticate_user!`, `before_action :ensure_admin!`, and the private `ensure_admin!` method.
    4. Create `app/views/layouts/admin.html.erb`:
       - Basic HTML structure with a "sewaLink Admin" header and `<%= yield %>`.
       - Include standard stylesheets and javascript tags.
    5. Update `config/routes.rb`:
       - Add `get '/', to: 'dashboards#show'` inside the `namespace :admin` block.
  </action>
  <verify>
    <automated>bundle exec rake routes | grep admin</automated>
  </verify>
  <done>Admin infrastructure created and PayoutsController refactored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Setup Admin Request Specs</name>
  <files>spec/requests/admin/dashboards_spec.rb, spec/factories/users.rb</files>
  <behavior>
    - User factory has an 'admin' trait.
    - Admin user can GET /admin successfully (200 OK).
    - Regular user is redirected from /admin to root.
    - Unauthenticated user is redirected from /admin to login.
  </behavior>
  <action>
    1. Update `spec/factories/users.rb` to add `trait :admin do admin { true } end`.
    2. Create `spec/requests/admin/dashboards_spec.rb`:
       - Test access control for `GET /admin`.
       - Use `sign_in` helper from Devise (ensure rails_helper.rb includes Devise::Test::IntegrationHelpers).
  </action>
  <verify>
    <automated>bundle exec rspec spec/requests/admin/dashboards_spec.rb</automated>
  </verify>
  <done>Admin access control verified with automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin Space | Transition from public/user space to admin-only namespace. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | Admin::DashboardsController | mitigate | Strictly enforce `ensure_admin!` in Admin::BaseController. |
| T-07-01-02 | Elevation of Privilege | Admin Namespace | mitigate | Ensure all controllers in the admin namespace inherit from Admin::BaseController. |
</threat_model>

<verification>
1. Run `bundle exec rspec spec/requests/admin/dashboards_spec.rb` to confirm access control.
2. Manually check `/admin` after logging in as admin to verify layout rendering.
</verification>

<success_criteria>
- Admin infrastructure is decoupled and centralized.
- Unauthorized access to admin namespace is strictly prevented.
- Analytics dependencies are ready for implementation in subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-panel-and-analytics/07-01-SUMMARY.md`
</output>
