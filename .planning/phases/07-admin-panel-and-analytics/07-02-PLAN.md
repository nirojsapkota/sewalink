---
phase: 07-admin-panel-and-analytics
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified: [config/application.rb, config/importmap.rb, app/javascript/application.js, app/controllers/admin/dashboards_controller.rb, app/views/admin/dashboards/show.html.erb, spec/requests/admin/dashboards_spec.rb]
autonomous: true
requirements: [ADMIN-01]

must_haves:
  truths:
    - "Admin dashboard shows 'New Users', 'Tasks Completed', and 'Daily GMV' charts"
    - "Charts reflect data from the last 30 days"
    - "Charts use Chartkick and render correctly without JS errors"
    - "Application timezone is set to Kathmandu for accurate reporting"
  artifacts:
    - path: "app/controllers/admin/dashboards_controller.rb"
      provides: "Analytics data aggregation"
    - path: "app/views/admin/dashboards/show.html.erb"
      provides: "Interactive analytics visualization"
    - path: "config/application.rb"
      provides: "Correct timezone configuration for Nepal"
  key_links:
    - from: "app/views/admin/dashboards/show.html.erb"
      to: "app/controllers/admin/dashboards_controller.rb"
      via: "instance variables (@new_users_by_day, etc.)"
    - from: "app/views/admin/dashboards/show.html.erb"
      to: "chartkick.js"
      via: "line_chart helper"
---

<objective>
Implement initial growth analytics (Users, Tasks, GMV) in the Admin Dashboard using Chartkick.

Purpose: Provide administrators with high-level platform growth visibility.
Output: Integrated charts for user registration, task completion, and gross merchandise volume (GMV).
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-panel-and-analytics/07-01-SUMMARY.md
@.planning/phases/07-admin-panel-and-analytics/07-RESEARCH.md

<interfaces>
Admin::DashboardsController (current):
```ruby
class Admin::DashboardsController < Admin::BaseController
  def show
    # Placeholder for analytics data
  end
end
```

Task Statuses:
`enum status: { draft: 0, open: 1, assigned: 2, in_progress: 3, pending_payment: 4, payment_completed: 5, completed: 6, dispute: 7, cancelled: 8 }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Timezone and Chartkick Assets</name>
  <files>config/application.rb, config/importmap.rb, app/javascript/application.js</files>
  <action>
    1. Set `config.time_zone = "Kathmandu"` in `config/application.rb` to ensure `groupdate` aggregations align with Nepal time.
    2. Pin `chartkick` and `chart.js` (or `Chart.bundle`) in `config/importmap.rb`. Use: `bin/importmap pin chartkick chart.js`.
    3. Import `chartkick` and `chart.js` in `app/javascript/application.js`:
       ```javascript
       import "chartkick"
       import "chart.js"
       ```
  </action>
  <verify>
    <automated>grep 'config.time_zone = "Kathmandu"' config/application.rb && grep 'pin "chartkick"' config/importmap.rb</automated>
  </verify>
  <done>Infrastructure for charts and time-accurate reporting is ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Growth Metrics in Controller</name>
  <files>app/controllers/admin/dashboards_controller.rb, spec/requests/admin/dashboards_spec.rb</files>
  <behavior>
    - `@new_users_by_day`: Hash of `{ date => count }` for users created in the last 30 days.
    - `@tasks_completed_by_day`: Hash of `{ date => count }` for tasks with status 'completed' and `completed_at` in the last 30 days.
    - `@daily_gmv`: Hash of `{ date => sum_of_budgets }` (in NPR) for completed tasks in the last 30 days.
  </behavior>
  <action>
    1. Update `Admin::DashboardsController#show`:
       - Use `User.group_by_day(:created_at, last: 30).count`.
       - Use `Task.completed.group_by_day(:completed_at, last: 30).count`.
       - Use `Task.completed.group_by_day(:completed_at, last: 30).sum("budget_cents / 100.0")`.
    2. Update `spec/requests/admin/dashboards_spec.rb`:
       - Seed a few users and completed tasks with different `created_at`/`completed_at` dates.
       - Verify that the controller assigns variables with expected data points.
  </action>
  <verify>
    <automated>bundle exec rspec spec/requests/admin/dashboards_spec.rb</automated>
  </verify>
  <done>Growth metrics are accurately calculated and verified with tests.</done>
</task>

<task type="auto">
  <name>Task 3: Render and Style Growth Charts</name>
  <files>app/views/admin/dashboards/show.html.erb</files>
  <action>
    1. Update `app/views/admin/dashboards/show.html.erb`:
       - Replace the "Growth (Coming Soon)" placeholder with a grid of three charts.
       - Render "New Users" using `line_chart @new_users_by_day, title: "New Users (30d)", xtitle: "Date", ytitle: "Users"`.
       - Render "Tasks Completed" using `line_chart @tasks_completed_by_day, title: "Tasks Completed (30d)", xtitle: "Date", ytitle: "Tasks"`.
       - Render "Daily GMV (NPR)" using `line_chart @daily_gmv, title: "Daily GMV (30d)", xtitle: "Date", ytitle: "NPR", prefix: "Rs. "`.
    2. Wrap each chart in a `bg-white p-4 rounded shadow` container for a clean dashboard look.
  </action>
  <verify>
    <automated>grep 'line_chart @new_users_by_day' app/views/admin/dashboards/show.html.erb</automated>
  </verify>
  <done>Analytics dashboard is visually complete and interactive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Data Analytics | Exposure of platform-wide trends to administrators. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | Admin Dashboard Charts | mitigate | Ensure metrics are only accessible via authenticated admin session (inherited from Admin::BaseController). |
| T-07-02-02 | Resource Exhaustion | Analytics Queries | mitigate | Use `groupdate` which optimizes SQL groupings; 'last: 30' limit prevents scanning entire tables. |
</threat_model>

<verification>
1. Run `bundle exec rspec spec/requests/admin/dashboards_spec.rb` to verify data aggregation logic.
2. Visit `/admin` as an admin user and verify that three charts render correctly with data.
</verification>

<success_criteria>
- Three interactive charts are visible on the admin dashboard.
- Data accurately reflects the last 30 days of platform activity.
- Dashboard styling is consistent with the `admin` layout.
- Analytics aggregation is covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-panel-and-analytics/07-02-SUMMARY.md`
</output>
