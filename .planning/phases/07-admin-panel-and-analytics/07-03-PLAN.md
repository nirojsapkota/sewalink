---
phase: 07-admin-panel-and-analytics
plan: 03
type: execute
wave: 1
depends_on: ["07-01", "07-02"]
files_modified: ["config/routes.rb", "app/controllers/admin/users_controller.rb", "app/views/admin/users/index.html.erb", "app/views/admin/users/show.html.erb", "app/views/layouts/admin.html.erb", "spec/system/admin/user_management_spec.rb"]
autonomous: true
requirements: ["ADMIN-02"]
must_haves:
  truths:
    - "Admin can search users by phone number"
    - "Admin can view user details and account stats"
    - "Access is restricted to admin users"
  artifacts:
    - path: "app/controllers/admin/users_controller.rb"
      provides: "User management logic"
    - path: "app/views/admin/users/index.html.erb"
      provides: "User listing with search"
    - path: "app/views/admin/users/show.html.erb"
      provides: "User details and stats"
  key_links:
    - from: "app/views/admin/users/index.html.erb"
      to: "admin_user_path"
      via: "Show link"
---

<objective>
Implement the User Management interface for administrators to allow oversight of platform participants.

Purpose: Provide administrators with the ability to list, search, and audit user accounts and their marketplace activity.
Output: A functional admin user directory with search capabilities and detailed profile views including activity statistics.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-panel-and-analytics/07-02-SUMMARY.md
@app/controllers/admin/base_controller.rb
@app/models/user.rb
@app/models/task.rb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Admin User Routes and Controller</name>
  <files>config/routes.rb, app/controllers/admin/users_controller.rb</files>
  <action>
    - Add `resources :users, only: [:index, :show]` to the `admin` namespace in `config/routes.rb`.
    - Create `Admin::UsersController` inheriting from `Admin::BaseController`.
    - Implement `index` action with:
      - Ordering by `created_at DESC`.
      - Simple search filter by `phone` (using `params[:query]`).
      - Pagination using Kaminari (`.page(params[:page]).per(20)`).
    - Implement `show` action to load the user and calculate stats:
      - `@tasks_posted = @user.tasks.count`
      - `@tasks_completed_as_tasker = Task.joins(:accepted_bid).where(bids: { user_id: @user.id }).where(status: :completed).count`
      - `@tasks_completed_as_poster = @user.tasks.completed.count`
  </action>
  <verify>
    <automated>bundle exec rails routes | grep admin/users</automated>
  </verify>
  <done>Routes defined and controller actions implemented with search and stats logic.</done>
</task>

<task type="auto">
  <name>Task 2: Create Admin User Views and Update Navigation</name>
  <files>app/views/admin/users/index.html.erb, app/views/admin/users/show.html.erb, app/views/layouts/admin.html.erb</files>
  <action>
    - Add "Users" link to the navbar in `app/views/layouts/admin.html.erb`.
    - Create `app/views/admin/users/index.html.erb`:
      - Add a search form (text input for phone + Submit button).
      - Add a table listing users with columns: ID, Phone, Name, Active Role, Created At.
      - Add a "Show" link for each user.
      - Add pagination links at the bottom.
    - Create `app/views/admin/users/show.html.erb`:
      - Display user profile details: Phone, Name, Bio, Locale, Admin status.
      - Display a "Marketplace Activity" section with cards/stats for:
        - Total Tasks Posted.
        - Tasks Completed as Poster.
        - Tasks Completed as Tasker.
      - Add a "Back to Users" link.
    - Use Tailwind CSS for styling consistent with the admin dashboard.
  </action>
  <verify>
    <automated>ls app/views/admin/users/index.html.erb app/views/admin/users/show.html.erb</automated>
  </verify>
  <done>Admin user list and detail views created with search and activity stats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Automated Specs for User Management</name>
  <files>spec/system/admin/user_management_spec.rb</files>
  <behavior>
    - As an Admin:
      - I can visit `/admin/users` and see a list of users.
      - I can search for a user by phone and see the filtered results.
      - I can click "Show" on a user and see their details and stats.
    - As a regular user:
      - I am redirected to root if I try to access `/admin/users`.
  </behavior>
  <action>
    - Create a system spec `spec/system/admin/user_management_spec.rb`.
    - Use `FactoryBot` to create admin and regular users.
    - Test successful navigation and search.
    - Test unauthorized access prevention.
  </action>
  <verify>
    <automated>bundle exec rspec spec/system/admin/user_management_spec.rb</automated>
  </verify>
  <done>System specs pass, confirming admin-only access and functional user management interface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> Admin Panel | Only users with `admin: true` can cross this boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Elevation of Privilege | Admin::UsersController | Mitigate | Enforce `ensure_admin!` before_action in `Admin::BaseController`. |
| T-07-03-02 | Information Disclosure | Admin::UsersController | Mitigate | Ensure only admins can view sensitive user details (phone, etc.) via the restricted namespace. |
</threat_model>

<verification>
1. Login as Admin.
2. Navigate to Admin Panel -> Users.
3. Search for a user by phone.
4. View a user's details and verify stats (posted/completed counts) are accurate.
5. Try to access `/admin/users` as a non-admin and verify redirection.
</verification>

<success_criteria>
- Admin can browse all users with pagination.
- Admin can find specific users via phone search.
- User detail page provides a clear summary of their platform activity.
- Access control is strictly enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-panel-and-analytics/07-03-SUMMARY.md`
</output>
