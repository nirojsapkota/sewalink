---
phase: 07-admin-panel-and-analytics
plan: 04
type: execute
wave: 1
depends_on: ["07-03"]
files_modified: [
  "app/services/payments/ledger_manager.rb",
  "app/models/task.rb",
  "app/controllers/admin/tasks_controller.rb",
  "app/controllers/admin/disputes_controller.rb",
  "config/routes.rb",
  "app/views/admin/tasks/index.html.erb",
  "app/views/admin/disputes/index.html.erb",
  "app/views/admin/disputes/show.html.erb",
  "spec/system/admin/task_monitoring_spec.rb",
  "spec/system/admin/dispute_resolution_spec.rb"
]
autonomous: true
requirements: ["ADMIN-03", "ADMIN-04"]

must_haves:
  truths:
    - "Admin can view a paginated list of all tasks with status filters."
    - "Admin can view a dedicated list of tasks in 'dispute' status."
    - "Admin can see unmasked chat logs and evidence photos for a dispute."
    - "Admin can resolve a dispute by releasing funds to the tasker."
    - "Admin can resolve a dispute by refunding funds to the poster."
  artifacts:
    - path: "app/controllers/admin/disputes_controller.rb"
      provides: "Dispute resolution actions"
    - path: "app/services/payments/ledger_manager.rb"
      provides: "Refund logic for escrowed funds"
    - path: "app/views/admin/disputes/show.html.erb"
      provides: "Dispute detail view with evidence and chat"
  key_links:
    - from: "Admin::DisputesController#resolve"
      to: "Payments::LedgerManager.refund_poster"
      via: "Method call for refund decision"
    - from: "Admin::DisputesController#resolve"
      to: "Task#release_payment!"
      via: "AASM event for release decision"
---

<objective>
Implement the Task Oversight and Dispute Resolution interface for platform administrators.

Purpose: To ensure platform safety and trust by providing admins with tools to monitor marketplace activity and fairly resolve conflicts between users.
Output: Admin-only controllers and views for task management and dispute resolution, supported by extended ledger logic for refunds.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-panel-and-analytics/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Ledger and Prepare Models/Routes</name>
  <files>app/services/payments/ledger_manager.rb, app/models/task.rb, config/routes.rb, app/controllers/admin/tasks_controller.rb, app/controllers/admin/disputes_controller.rb</files>
  <action>
    - Add `refund_poster(task)` to `Payments::LedgerManager` that transfers the full escrow balance of a task back to `:user_external` with code `:refund`.
    - Update `app/models/task.rb` AASM configuration:
      - Add `:dispute` to the `from:` list for `release_payment` event.
      - Add `:dispute` to the `from:` list for `cancel` event.
    - Create `Admin::TasksController` and `Admin::DisputesController` inheriting from `Admin::BaseController`.
    - Register routes in `config/routes.rb`:
      - `resources :tasks, only: [:index, :show]` under `:admin` namespace.
      - `resources :disputes, only: [:index, :show]` under `:admin` namespace, adding a `post :resolve` member action.
  </action>
  <verify>
    <automated>bundle exec rails routes | grep admin/tasks && bundle exec rails routes | grep admin/disputes</automated>
  </verify>
  <done>Routes and controller skeletons are ready, and LedgerManager supports refunds.</done>
</task>

<task type="auto">
  <name>Task 2: Task Monitoring and Dispute Listings</name>
  <files>app/controllers/admin/tasks_controller.rb, app/controllers/admin/disputes_controller.rb, app/views/admin/tasks/index.html.erb, app/views/admin/disputes/index.html.erb</files>
  <action>
    - Implement `Admin::TasksController#index`:
      - Eager load associations (`:user`, `:category`, `:tasker`).
      - Support status filtering via `params[:status]`.
      - Paginate using `Kaminari`.
    - Create `app/views/admin/tasks/index.html.erb` with a table showing title, poster, tasker, budget, and status badges. Include status filter tabs.
    - Implement `Admin::DisputesController#index` to fetch tasks with `status: :dispute`.
    - Create `app/views/admin/disputes/index.html.erb` listing active disputes with direct links to the show page.
  </action>
  <verify>
    <automated># Manual verification via localhost:3000/admin/tasks if running, or via request spec</automated>
  </verify>
  <done>Admin can browse all tasks and specifically see all pending disputes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dispute Resolution Logic and Evidence View</name>
  <files>app/controllers/admin/disputes_controller.rb, app/views/admin/disputes/show.html.erb, spec/system/admin/dispute_resolution_spec.rb, spec/system/admin/task_monitoring_spec.rb</files>
  <behavior>
    - Admin Dispute Show: Displays all photos from `dispute_evidences`, and the full unmasked message history between poster and assigned tasker.
    - Release Action: Calling `resolve` with decision 'release' transitions task to 'completed' and triggers `LedgerManager.release_from_escrow`.
    - Refund Action: Calling `resolve` with decision 'refund' calls `LedgerManager.refund_poster` and transitions task to 'cancelled'.
  </behavior>
  <action>
    - Implement `show` action in `Admin::DisputesController`: load task, evidences, and chat messages.
    - Implement `resolve` action with decision-based branching as specified.
    - Create `app/views/admin/disputes/show.html.erb`:
      - Section for evidence photos and descriptions.
      - Section for chat logs (ensure `message.content` is used directly to bypass masking for admins).
      - "Release to Tasker" and "Refund to Poster" buttons with confirmation.
    - Write system specs to verify the resolution flow and ledger impact.
  </action>
  <verify>
    <automated>bundle exec rspec spec/system/admin/dispute_resolution_spec.rb</automated>
  </verify>
  <done>Dispute resolution is fully functional with automated test coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin Panel | Only accessible to users with `admin: true` flag. High-privilege actions (refunds) cross here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Elevation of Privilege | Admin::DisputesController | mitigate | Enforce `ensure_admin!` in `Admin::BaseController` (parent class). |
| T-07-04-02 | Insecure Direct Object Reference | Admin::DisputesController#resolve | mitigate | Ensure resolve action only works on tasks in `dispute` status. |
| T-07-04-03 | Information Disclosure | Admin::DisputesController#show | accept | Admins seeing unmasked PII in chat is required for mediation/investigation. Access is restricted to trusted personnel. |
</threat_model>

<verification>
Check that an admin can:
1. Navigate to /admin/tasks and filter by status.
2. Navigate to /admin/disputes and see a task in dispute.
3. Click into the dispute, view evidence and chat.
4. Successfully release or refund funds, and verify task status changes accordingly.
</verification>

<success_criteria>
- Admin can oversee all task lifecycles (ADMIN-03).
- Admin can resolve disputes with evidence and financial actions (ADMIN-04).
- System specs for monitoring and resolution pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-panel-and-analytics/07-04-SUMMARY.md`
</output>
