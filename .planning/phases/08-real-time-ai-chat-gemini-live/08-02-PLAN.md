---
phase: 08-real-time-ai-chat-gemini-live
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/gemini/live_service.rb
  - app/channels/ai/chat_channel.rb
  - config/routes.rb
  - app/controllers/live_chats_controller.rb
  - app/views/live_chats/show.html.erb
  - app/views/live_chats/_task_preview.html.erb
  - app/javascript/controllers/real_time_chat_controller.js
autonomous: true
requirements:
  - SAFE-03
must_haves:
  truths:
    - User can navigate to the live chat interface
    - AI can extract task details and show them in real-time as a preview
    - User can hear the AI's voice response
  artifacts:
    - path: app/controllers/live_chats_controller.rb
      provides: "Live chat interface route"
    - path: app/views/live_chats/show.html.erb
      provides: "Chat UI layout"
    - path: app/services/gemini/live_service.rb
      provides: "Tool execution and Turbo Stream broadcasting"
    - path: app/javascript/controllers/real_time_chat_controller.js
      provides: "Audio playback"
  key_links:
    - from: app/services/gemini/live_service.rb
      to: "Task Model"
      via: "execute_tool method"
      pattern: "Task.find_or_initialize_by"
    - from: app/javascript/controllers/real_time_chat_controller.js
      to: "AudioContext"
      via: "PCM playback queue"
      pattern: "createBufferSource"
---

<objective>
Implement tool execution for the Gemini Live integration and dynamic UI updates via Turbo Streams, enabling the AI to extract task details and show them to the user in real-time while playing back audio responses.

Purpose: This satisfies the requirement for an interactive, bidirectional AI chat where users can voice their service needs, and the AI systematically builds a task draft that the user can review before publishing (SAFE-03).
Output: Live chat interface with a real-time task preview and bidirectional audio.
</objective>

<context>
@.planning/phases/08-real-time-ai-chat-gemini-live/08-01-SUMMARY.md
@app/services/gemini/live_service.rb
@app/channels/ai/chat_channel.rb
@app/javascript/controllers/real_time_chat_controller.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Tool Execution and Turbo Streams in Gemini Service</name>
  <files>app/services/gemini/live_service.rb, app/channels/ai/chat_channel.rb</files>
  <action>
    - Update `AI::ChatChannel#subscribed` to pass `current_user` to `Gemini::LiveService.new` as a `user` argument.
    - In `Gemini::LiveService#initialize`, store `@user`.
    - Modify the `ws.on :message` block to properly route messages based on their payload keys (`server_content` vs `tool_call`).
    - If `data['tool_call']` is received, process the `function_calls` array.
    - For `name == 'create_task_draft'`:
      - Extract `args` (`title`, `description`, `budget`, `location`).
      - Parse `budget` and convert to `budget_cents` (budget * 100).
      - Use `@user.tasks.draft.last` or initialize a new task.
      - Assign fields. If `category` is nil, fallback to `Category.first`. Save the task.
      - Broadcast a Turbo Stream replacement: `Turbo::StreamsChannel.broadcast_replace_to(@user, :live_chat, target: "ai_task_preview", partial: "live_chats/task_preview", locals: { task: task })`.
      - Send a `tool_response` back to the WebSocket formatted properly for Gemini: `{"tool_response": {"function_responses": [{"id": call["id"], "name": call["name"], "response": {"result": "success", "task_id": task.id}}]}}`.
  </action>
  <verify>
    <automated>rails runner "puts Gemini::LiveService.method_defined?(:initialize) ? 'OK' : 'FAIL'"</automated>
  </verify>
  <done>Service parses tool calls, updates a draft task, broadcasts it via Turbo Streams, and replies to Gemini.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Live Chat Interface</name>
  <files>config/routes.rb, app/controllers/live_chats_controller.rb, app/views/live_chats/show.html.erb, app/views/live_chats/_task_preview.html.erb</files>
  <action>
    - Route: Add `resource :live_chat, only: [:show]` to `config/routes.rb`.
    - Controller: Create `LiveChatsController` with a `show` action ensuring `authenticate_user!` is called. Provide the latest draft task (if any) as `@task`.
    - View: Create `app/views/live_chats/show.html.erb` with:
      - A header for the Voice Assistant.
      - A container connected to the Stimulus controller (`data-controller="real-time-chat"`).
      - Buttons with `data-action="click->real-time-chat#startRecording"` and `stopRecording`.
      - A `<span data-real-time-chat-target="status">` indicator.
      - Turbo Stream listener: `<%= turbo_stream_from current_user, :live_chat %>`.
      - Turbo Frame or div target: `<div id="ai_task_preview"><%= render partial: 'task_preview', locals: { task: @task } %></div>`.
    - Partial: Create `app/views/live_chats/_task_preview.html.erb`. Render `title`, `description`, `budget`, and `location`. Include a visual indicator that it is a "Draft". If `task` is nil, render a placeholder state ("Describe your task to begin...").
  </action>
  <verify>
    <automated>rails routes | grep live_chat && ls app/views/live_chats/show.html.erb</automated>
  </verify>
  <done>Controller, views, and routes are configured to display the chat interface and the task preview target.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Audio Playback in Stimulus</name>
  <files>app/javascript/controllers/real_time_chat_controller.js</files>
  <action>
    - Expand `_onMessage(data)` to handle incoming audio chunks.
    - Identify chunks at `data.server_content.model_turn.parts[x].inline_data.data`.
    - The audio from Gemini is typically 24kHz, 16-bit PCM.
    - Initialize a playback `AudioContext` with `sampleRate: 24000` inside `startRecording` or lazily.
    - Implement a queuing mechanism (`nextPlayTime`) to schedule sequential playback of `AudioBufferSourceNode`s.
    - Convert Base64 string to an `ArrayBuffer`, then decode 16-bit PCM into the `Float32Array` required by `AudioBuffer`.
    - Schedule and play the buffers seamlessly.
  </action>
  <verify>
    <automated>grep -q "AudioBufferSourceNode" app/javascript/controllers/real_time_chat_controller.js && echo "OK"</automated>
  </verify>
  <done>Stimulus controller handles Base64 PCM payloads, decodes them, and plays back the AI's audio response continuously.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web Browser → AI::ChatChannel | User voice and WebSocket connections |
| Gemini API → Application | Tool calls originating from LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Elevation of Privilege | LiveChatsController | mitigate | Require `authenticate_user!` on the controller to ensure guest cannot bypass authentication. |
| T-08-02-02 | Spoofing | ChatChannel / LiveService | mitigate | Scope the Turbo Stream broadcast strictly to `current_user`'s channel identifier to prevent data leakage between users. |
| T-08-02-03 | Tampering | Gemini::LiveService | mitigate | Tool execution explicitly associates the draft task with the authenticated `@user`, preventing AI from modifying other users' tasks. |
</threat_model>

<verification>
- Ruby code syntax is valid.
- Turbo Stream channel names match between the controller view and the service broadcaster.
- The `create_task_draft` tool matches the expected properties of the `Task` model.
</verification>

<success_criteria>
- The live chat interface displays at `/live_chat`.
- The AI's tool calls successfully create or update a draft task in the database.
- The UI updates automatically via Turbo Streams without page reload.
- The browser plays back the AI's audio responses.
</success_criteria>

<output>
After completion, create `.planning/phases/08-real-time-ai-chat-gemini-live/08-02-SUMMARY.md`
</output>
