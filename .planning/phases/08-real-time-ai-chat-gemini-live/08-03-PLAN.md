---
phase: 08
plan: 03
type: execute
wave: 1
depends_on: ["08-02"]
files_modified: [
  "app/javascript/controllers/chat_controller.js",
  "app/views/messages/_message.html.erb",
  "app/models/message.rb",
  "app/models/conversation.rb",
  "app/controllers/messages_controller.rb",
  "app/views/conversations/show.html.erb",
  ".planning/STATE.md"
]
autonomous: true
requirements: ["SAFE-06", "SAFE-05"]
must_haves:
  truths:
    - "Chat bubbles use Tailwind classes for alignment and color, removing inline styles."
    - "Unmasked content is never transmitted in the public Turbo Stream (SAFE-06 security fix)."
    - "Authorized participants receive unmasked content via private Turbo Streams."
    - "STATE.md reflects project completion and ready-for-release status."
  artifacts:
    - path: "app/javascript/controllers/chat_controller.js"
      provides: "Tailwind-based styling logic"
    - path: "app/controllers/messages_controller.rb"
      provides: "Secure dual-broadcast logic"
  key_links:
    - from: "app/controllers/messages_controller.rb"
      to: "Turbo::StreamsChannel"
      via: "broadcast_update_to"
---

<objective>
Final UI and messaging polish for v1.0. 
This plan resolves remaining chat bubble styling issues, secures the unmasking logic to prevent sensitive data leaks, and updates project documentation to reflect completion.

Purpose: Ensure a production-ready, secure, and visually consistent messaging experience.
Output: Cleaned-up Stimulus controllers, secure real-time broadcasts, and finalized state documentation.
</objective>

<execution_context>
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/niroj/projects/project_ideas/sewaLink/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/javascript/controllers/chat_controller.js
@app/controllers/messages_controller.rb
@app/views/messages/_message.html.erb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat UI & Styling Cleanup</name>
  <files>app/javascript/controllers/chat_controller.js, app/views/messages/_message.html.erb</files>
  <action>
    1. Refactor `chat_controller.js`:
       - Remove all inline `style` assignments (backgroundColor, color).
       - Use Tailwind classes exclusively in `styleMessage`:
         - Sender: `bg-blue-600`, `text-white`, `rounded-br-none`, `justify-end`.
         - Receiver: `bg-gray-200`, `text-gray-900`, `rounded-bl-none`, `justify-start`.
       - Remove the `unmaskIfNeeded` method and its calls from `processMessages`.
    2. Update `app/views/messages/_message.html.erb`:
       - Remove `data-content="<%= message.content %>"` to fix the security leak.
       - Remove `data-is-poster`, `data-assigned-tasker-id`, and `data-task-assigned` as they are no longer used by the client.
       - Add `id="<%= dom_id message %>_text"` to the `<p class="message-text">` element to allow targeted Turbo Stream updates.
  </action>
  <verify>
    <automated>grep -v "data-content" app/views/messages/_message.html.erb && grep "id=\"<%= dom_id message %>_text\"" app/views/messages/_message.html.erb</automated>
  </verify>
  <done>Chat styling is Tailwind-native and sensitive data is removed from the DOM.</done>
</task>

<task type="auto">
  <name>Task 2: Secure Real-time Unmasking Logic</name>
  <files>app/models/message.rb, app/models/conversation.rb, app/controllers/messages_controller.rb, app/views/conversations/show.html.erb</files>
  <action>
    1. `app/models/message.rb`:
       - Update `viewer_aware_content(viewer)` to return unmasked `content` if `viewer` is an `authorized_participant?` (Poster or Assigned Tasker).
    2. `app/models/conversation.rb`:
       - Add a helper method `other_participant(user)` that returns the participant who is NOT the given user.
    3. `app/views/conversations/show.html.erb`:
       - Add `<%= turbo_stream_from current_user %>` near the top of the messages container to subscribe to private updates.
    4. `app/controllers/messages_controller.rb`:
       - In the `create` action, after the public broadcast to `@conversation`:
         - Broadcast an unmasked update to the `current_user` (sender): `Turbo::StreamsChannel.broadcast_update_to(current_user, target: "#{ActionView::RecordIdentifier.dom_id(@message)}_text", html: @message.content)`.
         - Determine the `other_participant`. If they are an `authorized_participant?` for this message, broadcast the same unmasked update to their private stream.
  </action>
  <verify>
    <automated>grep "broadcast_update_to" app/controllers/messages_controller.rb && grep "turbo_stream_from current_user" app/views/conversations/show.html.erb</automated>
  </verify>
  <done>Unmasking is handled securely via private server-to-client broadcasts.</done>
</task>

<task type="auto">
  <name>Task 3: Final Documentation & State Update</name>
  <files>.planning/STATE.md</files>
  <action>
    Update `.planning/STATE.md` to reflect the final state of v1.0:
    - Move "Messaging UI Reliability" from `Blockers / Risks` to `Success Markers`.
    - Mark Phase 8 as complete in the `Current Position`.
    - Update `progress.percent` to 100%.
    - Update `status` to "v1.0 Release Ready".
    - Clear out the `Pending Todos` related to Messaging UI.
  </action>
  <verify>
    <automated>grep "percent: 100" .planning/STATE.md</automated>
  </verify>
  <done>Project state reflects 100% completion of v1.0 requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → All Participants | Shared Turbo Stream channel (`@conversation`) |
| Server → Authenticated User | Private Turbo Stream channel (`current_user`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Information Disclosure | `_message.html.erb` | mitigate | Remove `data-content` attribute; unmasked data only sent via private streams. |
| T-08-03-02 | Information Disclosure | `MessagesController` | mitigate | Explicitly check `authorized_participant?` before broadcasting unmasked content to the recipient. |
</threat_model>

<verification>
1. Send a message with a phone number (e.g., 9841234567).
2. Verify in the DOM that the message text is initially masked for the recipient (if not assigned).
3. Verify that `data-content` is NOT present in the HTML.
4. Verify that the sender sees the unmasked number immediately after sending.
5. Assign the task and verify the recipient now sees unmasked content in new messages.
</verification>

<success_criteria>
- Chat bubbles styled correctly without inline styles.
- No sensitive data (phone/email) leaked in HTML source/data attributes.
- Real-time unmasking works for authorized users via private Turbo Streams.
- STATE.md shows 100% completion.
</success_criteria>

<output>
After completion, create `.planning/phases/08-real-time-ai-chat-gemini-live/08-03-SUMMARY.md`
</output>
