---
phase: 12-redesign-the-ui-to-look-more-professional-and-polished-not-g
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - app/views/admin/accounting/_subnav.html.slim
  - app/views/admin/accounting/dashboards/category.html.slim
  - app/views/admin/accounting/dashboards/show.html.slim
  - app/views/admin/accounting/ledger_entries/index.html.slim
  - app/views/admin/accounting/ledger_entries/show.html.slim
  - app/views/admin/accounting/reports/show.html.slim
  - app/views/admin/accounting/settlements/index.html.slim
  - app/views/admin/accounting/settlements/new.html.slim
  - app/views/admin/accounting/settlements/show.html.slim
  - app/views/admin/dashboards/show.html.slim
  - app/views/admin/disputes/index.html.slim
  - app/views/admin/disputes/show.html.slim
  - app/views/admin/payouts/index.html.slim
  - app/views/admin/roles/index.html.slim
  - app/views/admin/settings/show.html.slim
  - app/views/admin/bids/index.html.slim
  - app/views/admin/categories/_form.html.slim
  - app/views/admin/categories/edit.html.slim
  - app/views/admin/categories/index.html.slim
  - app/views/admin/categories/new.html.slim
  - app/views/admin/tasks/edit.html.slim
  - app/views/admin/tasks/index.html.slim
  - app/views/admin/tasks/show.html.slim
  - app/views/admin/users/edit.html.slim
  - app/views/admin/users/index.html.slim
  - app/views/admin/users/show.html.slim
autonomous: true
requirements: ["N/A — visual redesign phase, no formal REQUIREMENTS.md IDs (see 12-RESEARCH.md phase_requirements)"]

must_haves:
  truths:
    - "Admin panel uses the same slate/indigo color tokens and shared button/badge/input helpers as the rest of the app"
    - "Admin table/filter-bar/sidebar STRUCTURE is unchanged — only color/typography/spacing tokens are swapped (D-10 priority 4, tokens-and-primitives-only pass, no full redesign)"
    - "The 3 admin files with residual purple hex (dashboards/show, disputes/show, accounting/reports/show) have zero hex remaining"
    - "gray-* Tailwind classes across all 26 admin view files are converted to the equivalent slate-* classes for palette consistency"
  artifacts:
    - path: "app/views/admin/dashboards/show.html.slim"
      provides: "Admin dashboard with slate/indigo tokens, zero purple hex"
    - path: "app/views/admin/users/index.html.slim"
      provides: "Admin user table with slate token pass, existing table structure preserved"
  key_links:
    - from: "app/views/admin/dashboards/show.html.slim"
      to: "app/helpers/application_helper.rb"
      via: "button_class / status_badge_class calls"
      pattern: "button_class|status_badge_class"
---

<objective>
Apply a tokens-and-primitives-only light pass to the admin panel (D-10 priority 4, lowest priority per 12-CONTEXT.md and 12-UI-SPEC.md's explicit "Admin Panel" scope section). This is Wave 4 (final) of 4.

Purpose: Keep the admin panel visually consistent with the rest of the redesigned app (same slate/indigo tokens, same button/badge/input helpers) WITHOUT restructuring admin-specific layouts (tables, filter bars, sidebar) — that would be scope creep beyond what D-10/UI-SPEC approved for the admin panel.
Output: All 26 admin view files use slate/indigo tokens and shared helpers on their EXISTING structure; the 3 files with residual purple hex (`dashboards/show`, `disputes/show`, `accounting/reports/show`) are fully converted; the other 23 files (currently using plain `gray-*` Tailwind classes, no hex) are converted from `gray-*` to `slate-*` for palette consistency.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-redesign-the-ui-to-look-more-professional-and-polished-not-g/12-CONTEXT.md
@.planning/phases/12-redesign-the-ui-to-look-more-professional-and-polished-not-g/12-UI-SPEC.md

**Binding scope constraint (UI-SPEC.md "Admin Panel" section, D-10 priority 4, D-13):** Admin gets a tokens-and-primitives-only pass. Apply new color tokens (slate/indigo instead of purple/gray), the shared button/badge/card/input helper methods, and the Inter font (already inherited automatically from Plan 01's `@theme` change — no per-file font edit needed here). Do NOT redesign admin-specific layouts (tables, filter bars, sidebar structure) beyond swapping color/typography/spacing tokens onto the existing structure. Do NOT add new UI elements, do NOT change table column structure, do NOT change form field layout/order.

<interfaces>
<!-- Helper methods from Plan 01 (app/helpers/application_helper.rb) -->
```ruby
def button_class(variant = :primary); end   # :primary | :secondary | :destructive | :ghost
def card_class(interactive: false); end
def input_class; end
def status_badge_class(status); end
def admin_nav_link_class(path); end   # already restyled in Plan 01, no changes needed here
```
</interfaces>

## Gray-to-Slate Token Map (admin-specific — most of these 26 files use plain Tailwind `gray-*`, not hex)

| Old pattern | New pattern |
|---|---|
| `bg-gray-50` | `bg-slate-50` |
| `bg-gray-100` | `bg-slate-100` |
| `bg-gray-200` | `bg-slate-200` |
| `bg-gray-800` | `bg-slate-900` |
| `text-gray-300` | `text-slate-300` |
| `text-gray-400` | `text-slate-400` |
| `text-gray-500` | `text-slate-500` |
| `text-gray-600` | `text-slate-600` |
| `text-gray-700` | `text-slate-700` |
| `text-gray-800` | `text-slate-900` |
| `text-gray-900` | `text-slate-900` |
| `border-gray-200` | `border-slate-200` |
| `border-gray-300` | `border-slate-300` |
| `hover:bg-gray-50/100/200` | `hover:bg-slate-50/100/200` (matching shade) |
| `focus:ring-blue-*` (if present on inputs) | `focus:ring-indigo-600` (per input_class convention) |
| `bg-blue-100 text-blue-800` (old "open" status badge) | use `status_badge_class(status)` helper instead |
| `bg-indigo-100 text-indigo-800` (old "assigned" badge) | use `status_badge_class(status)` helper instead |
| `bg-yellow-100 text-yellow-800` (old "in_progress" badge) | use `status_badge_class(status)` helper instead |
| `bg-orange-100 text-orange-800` (old "pending_payment" badge) | use `status_badge_class(status)` helper instead |
| `bg-green-100 text-green-800` (old "completed" badge) | use `status_badge_class(status)` helper instead |
| `bg-red-100 text-red-800` (old "dispute" badge) | use `status_badge_class(status)` helper instead |

## Purple-Hex Migration Map (only 3 files need this: `admin/dashboards/show.html.slim`, `admin/disputes/show.html.slim`, `admin/accounting/reports/show.html.slim`)

| Old pattern | New pattern |
|---|---|
| `#7C3AED` | `indigo-600` |
| `#6D28D9` | `indigo-700` |
| `#4C1D95` (+ opacity suffixes) | `slate-900`/`slate-700`/`slate-600`/`slate-400`/`slate-200`/`slate-100` per opacity level, same mapping as Plans 01-03 |
| `#FAF5FF` | `white` or `slate-50` |
| `hover:-translate-y-*`, `active:scale-*`, `backdrop-blur-*`, `shadow-2xl` | remove entirely |
| `font-black`/`font-extrabold`/`font-bold` | `font-semibold` |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin accounting subtree (9 files)</name>
  <files>app/views/admin/accounting/_subnav.html.slim, app/views/admin/accounting/dashboards/category.html.slim, app/views/admin/accounting/dashboards/show.html.slim, app/views/admin/accounting/ledger_entries/index.html.slim, app/views/admin/accounting/ledger_entries/show.html.slim, app/views/admin/accounting/reports/show.html.slim, app/views/admin/accounting/settlements/index.html.slim, app/views/admin/accounting/settlements/new.html.slim, app/views/admin/accounting/settlements/show.html.slim</files>
  <read_first>
    app/views/admin/accounting/_subnav.html.slim
    app/views/admin/accounting/dashboards/category.html.slim
    app/views/admin/accounting/dashboards/show.html.slim
    app/views/admin/accounting/ledger_entries/index.html.slim
    app/views/admin/accounting/ledger_entries/show.html.slim
    app/views/admin/accounting/reports/show.html.slim
    app/views/admin/accounting/settlements/index.html.slim
    app/views/admin/accounting/settlements/new.html.slim
    app/views/admin/accounting/settlements/show.html.slim
    app/helpers/application_helper.rb
  </read_first>
  <action>
Apply the Gray-to-Slate Token Map to all 9 files (plain class-value swap, `gray-*` → `slate-*`, on the EXISTING table/panel structure — do not restructure).
`app/views/admin/accounting/reports/show.html.slim` additionally contains purple hex — apply the Purple-Hex Migration Map to this one file specifically.
Additionally for all 9 files:
- Replace ad-hoc primary/secondary buttons (e.g. "Export", "New Settlement", "Import") with `button_class(:primary)` / `button_class(:secondary)`.
- Replace ad-hoc status/category pill classes with `status_badge_class(status)` where a task/transaction status is rendered.
- Keep table `<table>`/`<thead>`/`<tbody>` structure, column order, and filter-bar form field order exactly as-is — only swap class values.
  </action>
  <acceptance_criteria>
    - `grep -clE "#7C3AED|#4C1D95|#FAF5FF" app/views/admin/accounting/reports/show.html.slim` returns 0
    - `grep -clE "text-gray-|bg-gray-|border-gray-" app/views/admin/accounting/_subnav.html.slim app/views/admin/accounting/dashboards/category.html.slim app/views/admin/accounting/dashboards/show.html.slim app/views/admin/accounting/ledger_entries/index.html.slim app/views/admin/accounting/ledger_entries/show.html.slim app/views/admin/accounting/reports/show.html.slim app/views/admin/accounting/settlements/index.html.slim app/views/admin/accounting/settlements/new.html.slim app/views/admin/accounting/settlements/show.html.slim | grep -v ':0' | wc -l` returns 0
  </acceptance_criteria>
  <verify>
    <automated>bundle exec rspec spec/requests/admin/accounting --fail-fast</automated>
  </verify>
  <done>All 9 accounting admin views use slate/indigo tokens with zero residual gray-* or purple hex classes, with table/filter structure unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Admin main dashboard, disputes, payouts, roles, settings (6 files)</name>
  <files>app/views/admin/dashboards/show.html.slim, app/views/admin/disputes/index.html.slim, app/views/admin/disputes/show.html.slim, app/views/admin/payouts/index.html.slim, app/views/admin/roles/index.html.slim, app/views/admin/settings/show.html.slim</files>
  <read_first>
    app/views/admin/dashboards/show.html.slim
    app/views/admin/disputes/index.html.slim
    app/views/admin/disputes/show.html.slim
    app/views/admin/payouts/index.html.slim
    app/views/admin/roles/index.html.slim
    app/views/admin/settings/show.html.slim
    app/helpers/application_helper.rb
  </read_first>
  <action>
Apply the Gray-to-Slate Token Map to all 6 files.
`app/views/admin/dashboards/show.html.slim` and `app/views/admin/disputes/show.html.slim` additionally contain purple hex — apply the Purple-Hex Migration Map to these 2 files specifically.
Additionally for all 6 files:
- Replace ad-hoc primary/destructive action buttons (release/refund/split/reopen on disputes, role assign/revoke on roles, settings save) with `button_class(:primary)` / `button_class(:destructive)`.
- Replace ad-hoc status pill classes with `status_badge_class(status)` where applicable.
- Keep existing table/card/form STRUCTURE unchanged — class-value swap only, per the binding admin scope constraint in this plan's context.
  </action>
  <acceptance_criteria>
    - `grep -clE "#7C3AED|#4C1D95|#FAF5FF" app/views/admin/dashboards/show.html.slim app/views/admin/disputes/show.html.slim | grep -v ':0' | wc -l` returns 0
    - `grep -clE "text-gray-|bg-gray-|border-gray-" app/views/admin/dashboards/show.html.slim app/views/admin/disputes/index.html.slim app/views/admin/disputes/show.html.slim app/views/admin/payouts/index.html.slim app/views/admin/roles/index.html.slim app/views/admin/settings/show.html.slim | grep -v ':0' | wc -l` returns 0
  </acceptance_criteria>
  <verify>
    <automated>bundle exec rspec spec/requests/admin/dashboards_spec.rb spec/requests/admin/disputes_controller_spec.rb spec/requests/admin/roles_controller_spec.rb spec/requests/admin/settings_controller_spec.rb spec/system/admin --fail-fast</automated>
  </verify>
  <done>Admin dashboard, disputes, payouts, roles, and settings views use slate/indigo tokens with zero residual gray-* or purple hex, structure unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Admin categories, bids, tasks, users (11 files)</name>
  <files>app/views/admin/bids/index.html.slim, app/views/admin/categories/_form.html.slim, app/views/admin/categories/edit.html.slim, app/views/admin/categories/index.html.slim, app/views/admin/categories/new.html.slim, app/views/admin/tasks/edit.html.slim, app/views/admin/tasks/index.html.slim, app/views/admin/tasks/show.html.slim, app/views/admin/users/edit.html.slim, app/views/admin/users/index.html.slim, app/views/admin/users/show.html.slim</files>
  <read_first>
    app/views/admin/bids/index.html.slim
    app/views/admin/categories/_form.html.slim
    app/views/admin/categories/edit.html.slim
    app/views/admin/categories/index.html.slim
    app/views/admin/categories/new.html.slim
    app/views/admin/tasks/edit.html.slim
    app/views/admin/tasks/index.html.slim
    app/views/admin/tasks/show.html.slim
    app/views/admin/users/edit.html.slim
    app/views/admin/users/index.html.slim
    app/views/admin/users/show.html.slim
    app/helpers/application_helper.rb
  </read_first>
  <action>
Apply the Gray-to-Slate Token Map to all 11 files (no purple hex present in these, per 12-RESEARCH.md verified grep — gray-to-slate swap only):
- Replace form inputs in `categories/_form.html.slim`, `tasks/edit.html.slim`, `users/edit.html.slim` with `input_class`.
- Replace ad-hoc primary/destructive/secondary buttons (create/edit/delete/reorder categories, accept/reject/cancel bids, force-cancel/edit tasks, suspend/reactivate/edit users) with `button_class(:primary)` / `button_class(:destructive)` / `button_class(:secondary)`.
- Replace ad-hoc status pill classes with `status_badge_class(status)` in `tasks/index.html.slim`, `tasks/show.html.slim`, `bids/index.html.slim`, `users/index.html.slim` where applicable.
- Keep table structure, column order, category reordering UI, and form field order exactly as-is — class-value swap only.
  </action>
  <acceptance_criteria>
    - `grep -clE "text-gray-|bg-gray-|border-gray-" app/views/admin/bids/index.html.slim app/views/admin/categories/_form.html.slim app/views/admin/categories/edit.html.slim app/views/admin/categories/index.html.slim app/views/admin/categories/new.html.slim app/views/admin/tasks/edit.html.slim app/views/admin/tasks/index.html.slim app/views/admin/tasks/show.html.slim app/views/admin/users/edit.html.slim app/views/admin/users/index.html.slim app/views/admin/users/show.html.slim | grep -v ':0' | wc -l` returns 0
    - `grep -c "input_class" app/views/admin/categories/_form.html.slim` returns 1 or more
    - `grep -c "status_badge_class" app/views/admin/tasks/index.html.slim` returns 1 or more
  </acceptance_criteria>
  <verify>
    <automated>bundle exec rspec spec/requests/admin/bids_controller_spec.rb spec/requests/admin/categories_controller_spec.rb spec/requests/admin/tasks_controller_spec.rb spec/requests/admin/users_controller_spec.rb spec/system/admin --fail-fast</automated>
  </verify>
  <done>Admin categories, bids, tasks, and users views use slate/indigo tokens and shared helpers with zero residual gray-* classes; table/form structure unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin panel (`/admin/*`) | Pre-existing role-gated (`super_admin`/`accountant` via rolify, Phase 10) trust boundary — unchanged by this plan; only visual classes and existing-structure markup touched, no route/controller/authorization changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Elevation of Privilege | Admin action buttons (force-cancel, suspend, role assign/revoke, settlement import) | accept | This plan only changes button `class:` styling (visual) — it does not touch the underlying `button_to`/`link_to` `method:`, `data-turbo-confirm`, or controller authorization logic (`before_action` role checks from Phase 9/10). Verified via read_first before editing each file; no route/controller files are in this plan's `files_modified`. |
| T-12-07 | Tampering (XSS) | Admin table cells rendering user-supplied data (task titles, dispute descriptions, category names) | accept | Existing auto-escaping `=` output unchanged by this plan; only `class:` attribute values are modified, no `raw`/`html_safe` introduced. |
</threat_model>

<verification>
Run the scoped `bundle exec rspec` commands listed per-task, plus the full admin request/system suite at the end:
```
bundle exec rspec spec/requests/admin spec/system/admin --fail-fast
```
All must pass — these specs check admin authorization, CRUD behavior, and page rendering (not styling), so a pass confirms no functional regression from the token swap.

Manual visual check (mandatory): `bin/dev`, log in as super_admin, visit the accounting dashboard, disputes, categories, tasks, users, and roles pages. Confirm slate/indigo tokens replace the old purple-hex/gray mix, existing table/filter/sidebar structure is visually identical (just recolored), and no new purple remains anywhere in `/admin`.
</verification>

<success_criteria>
- All 26 admin view files use slate/indigo tokens; zero `gray-*` or arbitrary purple hex classes remain.
- The 3 files that had purple hex (`dashboards/show`, `disputes/show`, `accounting/reports/show`) are fully converted.
- Admin table/filter-bar/sidebar structure is visually unchanged (tokens-and-primitives-only, per D-10/UI-SPEC binding constraint) — no new elements, no reordered columns/fields.
- `bundle exec rspec spec/requests/admin spec/system/admin` passes.
- This is the final wave — after this plan, all 4 phase 12 waves are complete and a full manual visual pass against 12-UI-SPEC.md's 6 Checker Sign-Off dimensions should be performed before closing the phase.
</success_criteria>

<output>
After completion, create `.planning/phases/12-redesign-the-ui-to-look-more-professional-and-polished-not-g/12-04-SUMMARY.md`
</output>
