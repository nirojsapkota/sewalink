div class="mx-auto md:w-2/3 w-full"
  h1 class="font-semibold text-4xl text-slate-900"
    = t('.title', default: 'New Task')

  = render "form", task: @task

  = link_to t('.back', default: 'Back to tasks'), tasks_path, class: "#{button_class(:secondary)} mt-4"
