Your sewaLink verification code
=================================

Hi <%= @user.name %>,

Use the code below to verify your sign-in. It expires shortly, so enter it right away.

    <%= @code %>

If you didn't request this code, you can safely ignore this email.
